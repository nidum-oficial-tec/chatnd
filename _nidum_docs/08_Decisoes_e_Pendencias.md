# 08 — Decisões e Pendências

> **Para quem é:** quem retoma o projeto ou precisa decidir algo, e quer saber o que já foi decidido (e por quê) e o que ainda está em aberto.
> **Quando consultar:** antes de propor uma mudança de rumo ou reabrir uma discussão.

---

## Parte 1 — Decisões já tomadas (e o porquê)

| # | Decisão | Por quê |
|---|---|---|
| D1 | **Estender o fork do Open WebUI** (não criar do zero) | Reaproveita login, usuários, Storage, RAG, UI. Custo aceito: fork mais pesado de manter. |
| D2 | **Roteamento por um "motor invisível" (ChatND)** em vez de o usuário escolher o modelo | Simplicidade para o usuário; ele só vê uma IA, a da Nidum. |
| D3 | ~~**RAG modo documento-inteiro** (não só trechos)~~ — **SUPERADA em 16/07/2026** | *Registro original:* trechos davam respostas fragmentadas/incompletas; doc inteiro garante completude; custo (~US$0,12/pergunta) aceito. **O que mudou:** o documento inteiro **competia com a busca afinada e estourava o orçamento** — medido, **159.849 de 200.000** caracteres consumidos por três documentos inteiros, sobrando quase nada para os trechos. `MAX_DOCS_INTEIROS` passou a `0` no `chatnd 1.23.0` (commit `e7232ec2`, 16/07/2026) e a máquina segue no código, desligada. Descrição corrente em `03_Arquitetura_e_Motor.md`. **Em aberto, e não decidido aqui:** se a completude que o D3 buscava deve voltar por outro mecanismo — a Fase D (enumeração) ataca o mesmo problema por um caminho diferente. |
| D4 | **Layout dos arquivos no código da ferramenta**, não em prompt | Garante identidade da marca consistente; prompt/RAG não estilizam arquivo. |
| D5 | **Wrappers privados + só a ChatND pública** | Usuário comum vê só a ChatND; motores ficam ocultos. |
| D6 | **Controle de acesso por aprovação manual** (não OAuth) | `nidumbrasil.com.br` não é Google Workspace; e-mails não são contas Google. Admin aprova só `@nidumbrasil.com.br`. |
| D7 | **Nunca revelar o LLM** ao usuário | Identidade: é "a inteligência da Nidum", não "OpenAI/Anthropic". |
| D8 | **Geração de imagem via Gemini** (`imagen-4.0-generate-001`, predict) | O modelo padrão do Open WebUI dava 404; este funciona com billing pago. |
| D9 | **Tríade fonte/forma/fluxo só quando aplicável** (gate no classificador) | Respostas pareciam "treinamento corporativo". A tríade é da própria Fonte (*Silêncio, Vida e Liberdade*), mas só cabe em pedidos gerativos. |
| D10 | **Editorial: estender o fork, headless (API+chat), Storage S3 ao escalar** | Precisa de endpoints/DB/jobs; livros estouram o volume de 500 MB. |
| D11 | **Editorial: evitar libs AGPL** (PyMuPDF, ebooklib) | Risco para uso comercial; EPUB é montado/lido com zipfile+XML. |
| D12 | **Continuidade: tudo no nome da empresa, chaves no Bitwarden** | A operação precisa sobreviver à saída de qualquer pessoa. |
| D13 | **Migração de e-mail adiada** | Tudo está no `nidum.tec26@gmail.com` (provisório) até haver e-mail institucional. Recomendação aceita: usar e-mail genérico de função, não pessoal. |
| D14 | **Ibrand para títulos** (além do logotipo) | Decisão do usuário (o brandbook reserva Ibrand ao logotipo, mas foi sobreposto); Ibrand tem cobertura PT completa. |
| D15 | **Base em 7 coleções por tipo** (`nd-atas`, `nd-projetos`, `nd-fonte`, `nd-normas`, `nd-marca`, `nd-contratos`, `nd-externo`), saindo de 2 (FONTE/ACERVOS) | Permite roteamento de busca por tipo (busca só nas coleções relevantes) e etiquetas de procedência. `nd-externo` isola docs públicos de terceiros (IPPUL: leis municipais). |
| D16 | **Regra de destino: CONTEÚDO decide, NOME confirma, PASTA desempata**; soberania de pasta `FONTE/`→`nd-fonte` e `IPPUL/`→`nd-externo` **acima** do conteúdo | Procedência inviolável (uma ata citada dentro da doutrina não vira `nd-atas`; lei de Londrina não entra em coleção interna). Fonte única: `esteira/_scripts/colecao_destino.py`. |
| D17 | **`cte_/ct_/cc_/ce_/ger_/ata_` → `nd-atas`** (ata de comitê); `cte_` **nunca** `nd-contratos` | `cte_` é Comitê Técnico (ata), não contrato. `nd-contratos` só por nome (contrato/acordo/estatuto/cnpj) ou pasta jurídica. |
| D18 | **Exclusão de indexação** (`testes/`, `Chico/1 - Cadastros`, backups `antes-da-correcao`, stubs <80 chars, OCR corrompido) — nada sai do repo | Gabaritos, dado pessoal e lixo não devem poluir a base; a exclusão é só de indexação, reversível. Sem-regra vai para `nd-normas` com warning visível. |
| D19 | **Espelho nas 2 coleções antigas tem PRAZO** | Durante a transição, arquivo novo é indexado na coleção nova **e** nas 2 antigas (rede de rollback). Após o aceite da fase, as antigas **param de RECEBER** novos; **nunca são apagadas**. |
| D20 | **Pasta = "projeto"** (instruções + coleções por pasta), com o seletor de Conhecimento do modal de Pasta restrito a **coleções** (prop `collectionsOnly`, PR #36, 2026-08-23) | O recurso já existia no fork (OWUI 0.9.6: `folder.data.system_prompt` + `files`, que o upstream chama de *Folder "Project" handling*) — não foi construído, foi habilitado. A restrição existe porque a pasta aponta para **coleção inteira, nunca para arquivo solto**: com a leitura liberada ao grupo, o `GET /knowledge/search/files` passou a listar um a um os arquivos das coleções no seletor. **CUSTOMIZAÇÃO DE CORE** (`Knowledge/KnowledgeSelector.svelte`, `Models/Knowledge.svelte`, `Folders/FolderModal.svelte`): vai conflitar em **todo bump de versão do fork** — ver a REGRA DO REBASE no `CLAUDE.md`. Pastas são **individuais** (tabela `folder` tem `user_id` e nenhum `access_control`); projeto compartilhado por equipe seria desenvolvimento novo. |
| D21 | **Coleções institucionais em Privado com grant de LEITURA ao grupo Coautores** (não públicas, não escrita) | O usuário seleciona e usa, mas não adiciona nem remove arquivo: todas as rotas de mutação (`/file/add`, `/file/remove`, `/{id}/update`, `/{id}/delete`, `/{id}/reset`) exigem `permission='write'`; o export é admin-only. **EFEITO ALÉM DO SELETOR:** o pipe chama `filter_accessible_collections` antes de buscar — quem ficar fora do grupo perde o RAG da rota `documentos` **em silêncio** (log `nenhuma colecao de conhecimento acessivel`, resposta genérica em vez de erro). Ao mexer nos grants, conferir a cobertura do grupo. |
| D22 | **Consumidor generativo nas duas pontas: o que precisa ser exato é imposto pelo código, não pedido no texto** (03/09/2026) — vale para toda ferramenta nova daqui em diante | As ferramentas do ChatND foram desenhadas para um consumidor **determinístico**: o pipe montava a entrada por schema e repassava a saída *verbatim*. No laço agêntico o **modelo** escreve a entrada e transcreve a saída, e os três defeitos medidos na Fase A são o mesmo deslocamento, não três bugs: (1) o modelo reescreveu o link de download sem a barra inicial → 404; (2) mandou slides sem campo de corpo → PPTX mudo; (3) escreveu CSS próprio que venceu o da marca por cascata. **Evidência direta:** mesmo pedido, mesmo modelo, mesma base — o pipe gerou 18 slides com corpo cheio, o agente 10 slides mudos. A única diferença era **quem descreve a interface** (schema por extenso no prompt × docstring com 4 dos 8 tipos e corpo "opcional"). **Consequência de projeto:** link absoluto em vez de caminho relativo; guarda de cascata em vez de convenção; validação que recusa alto em vez de campo opcional; alias com telemetria e prazo em vez de tolerância permanente. É a mesma regra que pôs **as cores no código e o julgamento na skill** (D4). Corolário: **descrição de ferramenta é código** — muda com PR, entra na doc viva e tem caso de teste. |
| D23 | **A parede `geral` × `documentos` vale também para o agente — e o mecanismo é procedência declarada por afirmação, não separação de ferramentas** (03/09/2026, verificada no A.5-bis) | O FIRMADO do `CLAUDE.md` prevalece sobre a §3.2 do prompt-mestre. Mas numa arquitetura **sem rotas** a parede muda de forma: não é "só uma fonte por resposta", é **"não confunda a origem"**. O agente pode usar base e web na mesma resposta, em blocos separados e rotulados; o que é proibido é misturar as duas dentro da mesma afirmação ou parágrafo. **Verificado com a ferramenta na mão** (não por ausência dela): pedido que forçava a mistura — "compare o modelo de cooperativa que já definimos com o que a legislação brasileira mudou" — produziu seção interna, seção externa, e tabela com as colunas rotuladas; nenhuma afirmação misturada. **Ganho não previsto:** identificou uma contradição jurídica que nenhum dos lados continha sozinho (o modelo interno fala em folha CLT com encargos; a Lei 12.690/2012 diz que cooperado não tem vínculo), ligou a uma pendência já aberta nas atas e propôs três estruturas. A parede **não** custou capacidade analítica. **Regra da query (a mais importante):** conteúdo interno NUNCA entra no texto da busca externa — a query sai da casa. Medido: três buscas externas, zero menção à Nidum; a busca interna usou "modelo de cooperativa Nidum", as externas só termos públicos. **Destino do `ENABLE_WEB_SEARCH=False`:** era contorno técnico para contaminação pré-pipe (o caminho é pulado em Native, `middleware.py:2578`), não política — a decisão de 03/09 é ligar. E o valor efetivo mora no **banco**, não na env var: a variável só semeia a primeira carga (3ª vez que esse padrão aparece; ver D24). |
| D24 | **O valor efetivo de configuração mora no BANCO; env var e default de código só semeiam a primeira carga** (03/09/2026) | Apareceu **três vezes** no mesmo dia, sempre custando investigação: (1) `DIAL_FASE3` tem `default=False` no código e estava **ligado** em produção; (2) `BASE_CONHECIMENTO_ID` apontava para duas coleções apagadas enquanto o default do código era `""`; (3) `ENABLE_WEB_SEARCH=True` no Railway **não** ligou a busca — o interruptor "Pesquisa na Web" em Admin → Configurações estava desligado no banco, e o banco tem precedência. **Consequência:** ler o código NÃO diz o que está no ar, e nenhuma das três divergências deixa rastro em commit. Toda pergunta sobre estado corrente se responde no **painel**, não no repositório. É a origem comum dos treze registros vencidos (ver a lista no plano da Fase A) e a justificativa do `conferir_registros.py` — que precisa conferir também **o que a esteira carimba nos dados**, não só o que a doc afirma. |
| D25 | **A esteira protege só a SAÍDA. A premissa mudou: ingestão indevida também é irreversível — e passa a exigir freio de ENTRADA** (03/09/2026) | Os três freios da esteira calculam **sobre remoção**, sem exceção: `puxar_sharepoint.freio_disparou(**remover**, …)` (nº1), `sincronizar.freio_unificado(**removidos**, …)` — todos os ramos, `ESVAZIAR`/`CATASTROFE`/`EVENTOS` (nº2) — e `higiene_owui.freio_higiene(**n_exclusoes**, …)` (nº3). A `guarda_entrada`, apesar do nome, também é saída: pega "repo vazio + base cheia", que é remoção em massa disfarçada. **O único controle que olha a entrada é `relato_topo_ignorado`, e o próprio código diz: _"Nao bloqueia: e um aviso de escopo, nao um freio"_.** A assimetria não é esquecimento — é desenho replicado três vezes de propósito (`higiene_owui:60` diz "estilo dos freios nº1 e nº2"), e era **certa** quando a esteira nasceu do incidente da base zerada: o risco era **perder** dado. **O que mudou:** o risco virou **ingerir** o que não devia. E ingestão **também é irreversível** — o arquivo sai do índice, mas a leitura não se desvê: quem viu a carta ao presidente viu, e a resposta já dada não se recolhe. Os dois casos de 2026 são de entrada, não de saída: os 7 documentos "NÃO CIRCULAM" (17/08) [**CORRIGIDO em 04/09 — ver D32: este caso era falso positivo; o material e interno legitimo e os sete ficam**] e o risco vivo de `testes/` sair da exclusão na reformulação das pastas, que faria o ChatND responder com o gabarito do próprio banco de provas. **Feito:** `guarda_pastas_soberanas` no portão de abertura do `sincronizar` — trava a **indexação**, nunca o espelho do repo (a regra 9 se mantém: o SharePoint continua sendo a fonte da verdade). **Escopo da assimetria:** é da **esteira**, não do projeto — o pipe tem orçamento de entrada (`MAX_CHARS_TOTAL`, `MAX_CHARS_ANEXO`), a tool valida entrada (`_diag_entrada_vazia`, slide sem corpo) e o laço tem teto de iterações. **Em aberto:** um freio de VOLUME na entrada (adição em massa numa carga) continua não existindo em lugar nenhum. |
| D26 | **Toda mudança em interface ENTRE COMPONENTES começa pelo teste** (03/09/2026) — regra, não recomendação | Não é disciplina genérica de TDD: é uma regra sobre **um tipo específico de mudança**, e vem de placar medido num único dia. Ao trocar a entrega de arquivo por anexo nativo, o `teste_anexo_nativo.py` — escrito **antes** de publicar — pegou **três** defeitos que a revisão de código não pegou: (1) **contrato de prosa** — eu removi a frase `"Link para download:"` do retorno da tool, sem saber que o pipe faz `if "Link para download" in saida` (`chatnd.py:5773`) para decidir a oferta de outros formatos; teria desligado a oferta **em produção, em silêncio**; (2) **aliasing** — a lista de arquivos era devolvida por referência, e o payload do 1º evento ganhava o arquivo do 2º; (3) **parâmetro no meio da assinatura** — os dois novos campos deslocaram `imagens` para quem chama por posição, quebrando as suítes de imagem. **Por que só o teste pega:** interface entre componentes não está documentada em lugar nenhum — o contrato do "Link para download" existia como UMA linha de `if` num arquivo diferente, em outro módulo. Nenhuma leitura do arquivo alterado o encontraria; só exercitar o par encontra. **Corolário do D22** (o que precisa ser exato é imposto pelo código): o teste é a forma de descobrir **o que já estava sendo imposto** e você não sabia. **O que conta como interface entre componentes:** retorno de tool consumido por pipe ou por modelo; formato de carimbo lido por outro script; fatia/config duplicada entre repos; assinatura de função chamada por posição; nome de campo que o modelo preenche. **Vale também para dados**, e a mesma sessão provou: trocar as fixtures `ACERVOS/` por caminhos reais revelou que 9 de 19 caminhos do `mapa_assuntos.json` apontavam para pastas renomeadas — a etiqueta de assunto estava valendo zero havia semanas, com o `DIAL_FASE3` ligado. **Fixture que descreve um mundo que não existe é pior que teste ausente: ela dá confiança falsa e esconde o defeito que deveria denunciar.** |
| D27 | **Operação destrutiva roda simulação antes — independente de autorização** (04/09/2026) | Autorização é permissão para agir, **não dispensa de verificar**. Na remoção das 48 certidões o Davi autorizou executar direto; a simulação (`dry_run`) foi rodada mesmo assim e **pegou uma divergência**: o plano removia **2** arquivos de `nd-projetos` e a lista prevista tinha **1**. Os dois estavam certos (o segundo era um IPTU sob pasta `DUO DILIGENCE`), mas isso **só se soube conferindo** — antes da simulação, a diferença era indistinguível de um falso positivo prestes a apagar material legítimo. O custo da simulação são dois minutos; o custo de descobrir depois é irreversível. Vale para: remoção de índice, exclusão de arquivo, mudança de valve que afeta classificação, e qualquer `--confirmar-*`. |
| D28 | **Órfão de índice: as vias de nascimento, e por que "o freio avisa" não fecha o problema** (04/09/2026) | **Órfão** = arquivo no índice que o estado desejado não contém. Descoberto por acidente: o plano de remoção das certidões expôs `RTF-003 › Relatorio_Certidao_Imob`, **que estava no índice e não no repositório** — e junto vieram CNH, dois extratos bancários e três certidões, todos só no índice. Apareceram porque casavam com a regra nova; **órfão que não casa com regra nenhuma continua invisível**. **Vias mapeadas no código:** (1) **rodada barrada** — o freio aborta a rodada INTEIRA, nada é escrito, o repo anda e o índice congela; (2) **coleção PULADA** (`id "PREENCHER-APOS-CRIAR"`) — a rodada segue e as remoções daquela coleção nunca acontecem; (3) **falha de API na remoção** — levanta e aborta a rodada, mesma consequência de (1); (4) **`freio_mudanca_colecao`** — arquivo que troca de coleção pode ficar nas duas; (5) **vetores sem vínculo** — classe diferente, já coberta pelo `higiene_owui.py`. **A boa notícia:** (1), (3) e (4) são **auto-corrigíveis** — o plano é recalculado do zero a cada rodada, então a remoção pendente volta a ser planejada. **O que falha não é o plano, é a rodada não completar.** Foi o caso aqui: as coleções não recebiam escrita desde 22/08. **Por isso o alarme certo não é "houve bloqueio", é "não há rodada bem-sucedida há N horas"** — essa única checagem cobre freio, falha de API, workflow desabilitado e secret ausente, com um fato só. **DEFEITO ENCONTRADO no ciclo da issue:** o passo *"Rodada OK → fecha a issue de bloqueio"* (`sincronizar.yml:123`) **não tem guarda de `dry_run`**. Uma simulação com plano limpo **fecha a issue de bloqueio sem ter escrito nada** — a dívida sai do radar sem ter sido paga. Corrigir junto com o alarme de defasagem. **FECHADO em 04/09/2026 — ZERO órfãos.** O relatório rodou como Action (`orfaos.yml`, só leitura, credencial nos secrets) e as sete coleções batem exatamente com o repositório: **nd-atas 108, nd-projetos 17, nd-fonte 83, nd-normas 286, nd-marca 4, nd-contratos 5, nd-externo 20** — 523 arquivos, **nenhum com sinal de dado pessoal**. O resultado confirma a via (1) e só ela: não havia classe escondida nem defeito de plano, havia **acúmulo de 22/08 até 04/09**, drenado de uma vez quando a rodada completou com confirmação. **Órfão nasce de rodada que não completa, não de falha no plano** — e é isso que faz do alarme de defasagem o controle certo, e não um segundo varredor rodando em paralelo. Estas contagens são a **linha de base do que "limpo" significa**: a referência contra a qual o alarme e qualquer conferência futura se medem. (O defeito do `dry_run` foi corrigido no PR #147.) |
| D29 | **Credencial que já existe no lugar certo não se copia para o lugar conveniente** (04/09/2026) | Para rodar o relatório de órfãos, o primeiro passo a passo que escrevi pedia ao Davi definir `OPENWEBUI_BASE_URL` e `OPENWEBUI_API_KEY` no terminal dele. Os dois **já eram secrets do repositório desde 06/07** — os mesmos com que a esteira sincroniza todo dia. O runbook criava uma **segunda cópia** da credencial, num lugar sem rotação, sem auditoria e sem revogação centralizada, para fazer o que o GitHub já fazia. Correção: o relatório virou workflow manual (`orfaos.yml`) e ninguém manuseia token para investigar. **O teste que a regra impõe:** antes de pedir uma credencial a alguém, procurar onde ela já está — se existe num cofre que o executor alcança, o trabalho é **levar a execução até a credencial**, nunca a credencial até a execução. **Corolário operável:** runbook que começa com "cole o token" é sinal de que a execução está no lugar errado — trate como defeito de desenho, não como inconveniência. Palavras do Davi: _"eu ia criar uma segunda cópia da credencial num lugar sem rotação nem auditoria, para fazer o que o GitHub já fazia"_. |
| D30 | **Ferramenta de relatório: o achado não vai para o log, e encontrar não é falhar** (04/09/2026) | Duas decisões tomadas ao pôr o relatório de órfãos numa Action. Nenhuma é sobre órfãos — as duas são sobre **como um relatório se comporta**, e valem para o próximo. **(1) O nome do achado fica fora do log.** Órfão é, por definição, o arquivo que **não** está no repositório — e foi exatamente ali que apareceram CNH e extratos bancários. Nome de arquivo de dado pessoal **já é** dado pessoal: `CNH-<nome>.pdf` identifica a pessoa sem que ninguém abra o arquivo. E log de Action se lê de passagem, sem intenção e por qualquer um com acesso ao repo. O modo `--resumo` manda **só contagens** para o log; os nomes vão para o artefato, que exige download deliberado e expira em 3 dias. **(2) Job verde ao encontrar.** O código de saída passou a ser 0 = limpo, **2 = achou**, 1 = quebrou. Sair 1 nos dois casos era ambíguo (o `SystemExit` da variável ausente também sai 1), mas o motivo principal não é técnico: **relatório que fica vermelho ao cumprir a função treina todo mundo a ignorar o vermelho.** Vermelho tem de significar uma coisa só — a ferramenta não funcionou. O que ela **encontrou** vai para o Summary da página do run, legível sem abrir log nenhum. |
| D31 | **Alarme de defasagem: mede trabalho pendente, e o N deriva da cadência** (04/09/2026) | Fecha o vão do D28. **A pergunta é uma só** — _existe mudança esperando indexação há mais de N horas?_ — e cobre freio disparado, falha de API, workflow desabilitado e secret vencido, porque os quatro produzem o mesmo fato observável. **O que se mede importa mais que o limite:** medir _tempo desde a última rodada_ alarma em todo fim de semana quieto; a condição exige **commit de `.md` mais novo que a última escrita**, então silêncio legítimo nunca toca. Alarme que toca à toa é desligado — e alarme desligado ainda figura na doc como proteção, o que é pior que não ter. **N = 12h, com o número que o escolheu** (30 dias, medidos em 04/09 sobre o `puxar_sharepoint`, que é o caminho real da sincronização diária — o `sincronizar.yml` roda só 12 vezes no mesmo período): **133 rodadas, 26 falhas (19,5%), das quais só 3 ISOLADAS** (07/08, 12/08, 23/08); as outras 23 vieram em bloco de dias seguidos. Com rodada a cada 6h, **N=6h não tem margem de retentativa** — a falha isolada já deixa o commit pendente ~6h até a rodada seguinte consertar, e as 3 do mês virariam 3 falsos alarmes. **N=12h é um ciclo completo de retentativa.** Retrospectiva contra o histórico real: teria disparado em **24/08**, onze dias antes da descoberta, com zero disparos nos dias saudáveis. **Regra que fica:** N é derivado da cadência (2 × intervalo entre rodadas), não escolhido a gosto — se a cadência mudar, o N muda junto. **Canal:** issue única atribuída, não e-mail — notificação de workflow agendado vai para quem editou o arquivo por último, o que troca o destinatário por acidente a cada PR. A issue fecha sozinha na primeira rodada de escrita, então "aberta" significa uma coisa só. **Limite conhecido:** rodada bem-sucedida não é prova de escrita (um `puxar` sem delta sai cedo e conta como sucesso) — o alarme mede o processo, não o resultado; fechar de vez pede carimbo de escrita no `_estado/`. |
| D32 | **O caso “NÃO CIRCULAM” era falso positivo — e nome de pasta não é política de acesso** (04/09/2026, corrige a leitura vigente desde 17/08) | Os sete documentos da pasta `5 - Minutas a convergência - NÃO CIRCULAM` foram tratados por **três semanas** como incidente de acesso. Não eram. Levantado o contexto: `“NÃO CIRCULAM”` é instrução de **não distribuir para fora**, não de restringir acesso interno; quem marcou a pasta foi o **Sergio, CEO**; **todos os colaboradores têm visualização no SharePoint**; e quem usa o ChatND são esses mesmos colaboradores. **Não há ampliação de alcance** em indexar os sete — quem perguntaria ao ChatND já pode abrir os documentos hoje. Os sete **ficam** no índice e no repositório; nada a remover. **A lição, que é maior que o caso:** política de acesso mora no sistema de permissões, não no nome que alguém escreveu na pasta. Antes de tratar uma pasta com nome sugestivo como restrição, a pergunta é **quem tem permissão nela hoje** — se todo mundo tem, não há restrição a preservar. Um rótulo descreve intenção de quem escreveu; permissão descreve o fato. **Consequências:** PR #144 **fechado** (o código funcionava; a premissa é que estava errada), `eh_restrito` volta para a fila normal sem prioridade, `teste_restritos.py` fica segurado. **O que isto NÃO invalida:** as certidões, a CNH, os extratos e a árvore de DUE DILIGENCE eram **incidente real** — dado pessoal de terceiros, sem base para estar num índice que a empresa inteira consulta. As remoções dos PRs #141, #145, #146 e #150 seguem corretas. Só este caso era falso positivo. |
| D33 | **Número sem método envelhece virando folclore: todo número carrega data e método, ou não entra** (04/09/2026) — regra, não recomendação | Vale para comentário de código, doc viva, mensagem de commit e registro. **O caso concreto:** um comentário em `regras_governanca.py` afirmava _“medido contra 1426 nomes reais: 48 casamentos, nenhum falso positivo”_. Re-medindo no dia seguinte: **1166 nomes, 16 casamentos**. A queda é compatível com o que saiu do corpus (37 arquivos removidos, árvore de DUE DILIGENCE excluída), mas os dois levantamentos **não usam o mesmo método de extração** — então o par não é comparável termo a termo, e nenhum dos dois pode ser defendido sozinho. **Por que é pior que não ter número:** um número solto no código tem cara de prova. Ele encerra a discussão sem poder ser refutado, porque ninguém sabe como refazê-lo. **O formato mínimo:** _o quê, quanto, quando, sobre qual população, apurado como_. Se o método não couber na linha, cabe o ponteiro para o script que o produz. **Irmão do D24** (o valor efetivo mora no banco): lá a doc afirma configuração que ninguém conferiu; aqui, medição que ninguém consegue repetir. Os dois envelhecem em silêncio, e os dois se corrigem com a mesma disciplina: **afirmação em doc dura precisa dizer como foi verificada.** |
| D34 | **Antes de afirmar o que existe num branch, buscar — e o incidente que eu quase registrei** (05/09/2026) | Afirmei que `_f3_colecao` e `DIAL_FASE3` **não existiam na `main`** e concluí que o pipe de produção vinha de um branch marcado “não mergear/publicar” — ou seja, que a `main` não era a fonte da verdade do componente mais caro do sistema. **Falso.** O `origin/main` tem os dois, no mesmo arquivo e na mesma linha (`chatnd.py:3249`). O que eu tinha era um `main` **local 32 commits atrasado**, e um `git grep main` responde sobre a ref local, não sobre o remoto. **Não houve incidente de governança.** Consequências reais enquanto o erro esteve de pé: commitei um conserto no branch errado (revertido) e o Davi esteve a um passo de decidir entre “mergear o branch” e “republicar da main” para um problema inexistente. **A regra:** `git fetch` antes de qualquer afirmação sobre o que um branch contém, e preferir `origin/<branch>` a `<branch>` ao comparar — a ref local é uma foto com data, não o estado do projeto. **Irmão do D24 e do D33, e o mais desconfortável dos três:** lá a doc afirma configuração que ninguém conferiu e medição que ninguém repete; aqui a **ferramenta** respondeu com precisão sobre um mundo velho, e a precisão da resposta escondeu a idade da pergunta. **Registrado aqui, e não como incidente,** porque um incidente falso em doc dura é pior que doc ausente — daqui a seis meses alguém leria que a produção já rodou fora da `main` e agiria sobre isso. |
| D35 | **As bases do eixo novo ficam SEM descrição — com base = pasta, o nome já é a informação** (05/09/2026) | Foram escritas dez descrições (conteúdo, quando buscar, quando não buscar) e **nenhuma será aplicada**. **Por quê:** as bases vão crescer muito, e **descrição-inventário envelhece** — uma linha que lista o que a pasta contém hoje passa a mentir no dia em que alguém salva um arquivo diferente, e ninguém volta para corrigi-la. É o D33 aplicado a texto em vez de a número: afirmação sem manutenção vira folclore. E com o eixo por pasta o nome carrega o que precisa carregar — `Operações` diz o que `Operações` é. Sob o eixo por TIPO isso não valia, e por isso a descrição era necessária lá: `nd-normas` **não** dizia o que continha — dizia o contrário do que continha. **Trocar o eixo tornou a descrição dispensável, e não o inverso.** **A alternativa que fica para o futuro,** se a escolha de base pelo agente se mostrar ruim: **uma linha dizendo o que a pasta É, nunca o que ela contém** — definição de escopo, não inventário. Definição sobrevive ao crescimento; inventário não. **Como saber que chegou a hora:** o agente escolhendo a base errada de forma repetida em pergunta cuja pasta é óbvia para uma pessoa. |
| D36 | **Pasta-mãe que vai receber conteúdo ganha base ANTES, não depois** (05/09/2026) — reverte a decisão tomada horas antes | `Plataformas Regionais` tinha **um** arquivo, e ele era cópia de algo já indexado em Tecnologia. Com esse dado, decidi mantê-la **excluída**: criar base para hospedar uma cópia acrescentaria um nome a mais no `list_knowledge_bases` e uma chance a mais de o agente escolher errado, em troca de zero conteúdo novo. **O Davi reabriu, e está certo:** a pasta vai receber conteúdo próprio em breve, e *“a decisão de manter excluída foi tomada com o dado de hoje, e o dado de amanhã é diferente”*. **A regra que fica:** pasta-mãe vazia hoje que **vai** receber conteúdo ganha base **antes** — para o primeiro arquivo já entrar no lugar certo, em vez de cair na exclusão e só ser notado quando alguém procurar por ele e não achar. **O que eu errei, e vale mais que o caso:** decidi com o inventário de hoje uma questão que era sobre a **intenção de uso da pasta** — e essa informação não está no acervo, está em quem organiza o SharePoint. Contagem de arquivos responde “o que há”; não responde “o que vai haver”, e eu usei a primeira para responder a segunda. **Irmão do D35:** lá a decisão foi não descrever porque descrição-inventário envelhece; aqui, não decidir escopo por inventário pela mesma razão. **O único arquivo atual continua sendo cópia do que está em Tecnologia, e isso é a pasta refletida, não defeito** — a base espelha a pasta, e duplicata entre pastas é fato do SharePoint, não erro de roteamento. |
| D37 | **Conferência que compara zero com zero é verde permanente sem cobertura — e só rodar contra dado real, desconfiando do verde, pega** (05/09/2026) — palavras do Davi; a lição mais cara do conferidor | **O caso, e ele apareceu duas vezes no mesmo dia, por portas diferentes.** (1) A classe `base_indevida` procurava os nomes das pastas **declaradas como excluídas** dentro das contagens das coleções **configuradas**. Pasta excluída não tem coleção configurada, **por construção** — a interseção era sempre vazia. (2) A coleta lia `data.file_ids` de `/knowledge/{id}`, o campo **legado** que o `file/add` não atualiza: devolveria **zero** para toda base com arquivos vinculados, e zero é exatamente o valor que a classe considera correto. **Nos dois, a comparação era zero contra zero, e o resultado era verde permanente.** **Por que nenhuma revisão pega:** o código está certo linha a linha; o teste passa — as fixtures colocavam o mesmo nome nos dois lados, um mundo que não existe; e o relatório diz “nada encontrado”, que é a resposta que se espera de um sistema saudável. **Um teste com fixture inventada prova que o código faz o que o código faz.** **O que pega:** rodar contra dado real e, ao ver verde, **perguntar por que está verde**. Foi assim que os dois apareceram — o relatório voltou limpo e fui conferir o motivo do limpo. **A regra operável:** para toda conferência nova, escrever **o caso que a faria falhar** e prová-lo com dado do mundo real antes de confiar no verde; e desconfiar em especial de comparação cujo lado esperado é **vazio, zero ou ausente**, porque nela o acerto e a cegueira têm a mesma aparência. **Família:** irmão do D26 (fixture que descreve mundo inexistente dá confiança falsa) e do princípio de que **ferramenta de conferência que erra sozinha é pior que ferramenta ausente** — aqui ela não erra, ela **acerta sem olhar**, que é mais difícil de flagrar. |
| D38 | **Escrever a prova é a última conferência** (05/09/2026) — observação do Davi sobre o método, depois de o padrão se repetir dia após dia | **O padrão:** o defeito apareceu **enquanto se escrevia o registro**, e não enquanto se escrevia o código. Os casos que dá para apontar no documento: **04/09** — ao mapear as cinco vias de nascimento de órfão para o D28, apareceu que o passo *“Rodada OK → fecha a issue”* não tinha guarda de `dry_run`, e que a simulação prudente teria fechado a issue sem pagar a dívida; **05/09** — ao escrever o D37, ao invés de só citar o endpoint errado, fui conferir *por que* a classe estava verde e achei a segunda cegueira, a interseção vazia por construção; **05/09** — ao redigir o corpo do PR do conserto anterior, apareceu que a mensagem de “não conferida” culpava a credencial mesmo quando ela estava presente. **Por que funciona, e não é acaso:** escrever a prova obriga a responder *por que isto está certo* em vez de *isto parece certo*. A revisão lê o código e confirma que ele faz o que diz; a redação do registro obriga a defender que **o que ele diz é o que precisava ser feito** — e é nessa distância que o defeito mora. **O corolário prático:** o registro não é burocracia depois do trabalho; é **a última passada de conferência**, e por isso se escreve **antes** de dar a tarefa por encerrada, nunca depois. Um PR cujo corpo não consegue explicar por que a mudança está certa não está pronto para merge — mesmo com a suíte verde. **Fecha a família:** o D26 diz que teste pega o que leitura não pega; o D37 diz que rodar contra dado real pega o que teste não pega; este diz que **escrever pega o que rodar não pega** — porque rodar mostra o que acontece, e escrever obriga a dizer o que **deveria** acontecer. |
| D39 | **Renomear no painel não deixa rastro, e o nome de exibição é o que as pessoas leem — três leram “revogado” onde havia “renomeado”** (05/09/2026) — caso do D24 | O `MODELO_GERAL` apontava para `nidum-10---dia-a-dia`, e **três pessoas** concluíram, em momentos diferentes, que era default morto apontando para wrapper revogado. **Falso.** O wrapper nunca foi revogado: foi **renomeado** para **“Nidum 1.0 - Geral”**, e o **id permaneceu o mesmo**. Verificado abrindo o modelo no painel. **Nada havia para trocar nem apagar** — valve correta, default correto. **Por que os três erraram junto:** o id `...dia-a-dia` descreve um nome que **só existiu no passado**, e ninguém tem como saber disso lendo código ou doc. Renomear no painel não produz commit, nem diff, nem entrada em lugar nenhum: **o id vira um fóssil do nome antigo**, e quem lê o fóssil conclui que o objeto morreu. É o D24 numa forma pior, porque não há divergência a detectar — os dois lados estão certos, e só a **ligação entre eles** está ausente. **O que fica:** (a) onde a doc cita um id de modelo, cita também o **nome de exibição atual**, com a marca de que houve renome — feito no `04`; (b) o `conferir_registros` ganha a classe `modelo_renomeado`, que compara o id citado na doc com o nome de exibição do painel; (c) a regra geral: **id é estável e nome de exibição não é — então conclusão tirada de nome de exibição precisa ser conferida no painel antes de virar ação**, e conclusão tirada do id precisa lembrar que ele pode ser fóssil. **O custo real deste caso:** entrou em três listas de pendência, gerou três opções de conserto com custo estimado, e a resposta era abrir uma tela. |
| D40 | **A topologia real do Railway: cinco componentes, o que cada um é e o que custa** (auditoria de 09–10/09/2026, método: `railway status/variables/bucket info/postgres pitr status/metrics` + SQL só-leitura no Postgres) | O painel mostra nomes, não funções — e um corte de custo feito pelo nome erra. **(1) `ChatND`**, o Open WebUI: 1 worker uvicorn (default do `start.sh`; `UVICORN_WORKERS` não existe no Railway nem no Dockerfile); embedding por **API OpenAI** (`text-embedding-3-small`, valor efetivo no banco — D24); **reranker LOCAL** `BAAI/bge-reranker-base` (3,2 GB no volume, ~1,2 GB de RSS no worker); volume `nidum-platform-volume-YLGF` 5,8/20 GB. Era o custo dominante: 14,6 GB de RAM em média nos 3 dias antes de 09/09 (D41). **(2) `Postgres-EM0i`** (pg18 + pgvector, volume `postgres-volume-CGRl` 2,7/50 GB): banco de 667 MB, RAM média de 600 MB e pico 1,23 GB em 7 dias — dimensionado pelo uso, não por reserva (Railway cobra por GB-hora consumido; `limitOverride` nulo). **(3) `tts-thalita`**: imagem `travisvn/openai-edge-tts`, criada em 30/07/2026 na POC de voz (`motores/voz`); **zero tráfego em 7 dias**, 50 MB de RAM; o pipe usa OpenAI TTS (`TTS_BASE_URL` vazio → api.openai.com). Nada depende dele. **(4) bucket `stocked-cup-MSag`** (5,0 GB, 40.089 objetos): é o **arquivo WAL/pgBackRest do PITR** do Postgres — não guarda PPTX/HTML. Arquivos gerados vão para o R2 `nidum-arquivos` (`STORAGE_PROVIDER=s3`), com cópia local no volume que o provedor S3 nunca apaga. **(5) bucket `Postgres-PITR`**: **vazio** (0 objetos), criado 1 min antes do outro em 06/07/2026 ao habilitar o PITR — sobra sem função. **Sobras:** dois volumes Postgres **desanexados** e cobrados (`postgres-volume` 0,86 GB, de 02/07, primeira tentativa de Postgres; `postgres-volume-U9Rg` 0,95 GB, de 09/07 23:07 — o banco descartável do teste de restauração, cujo serviço já não existe). **Fatura** (CLI `railway usage`, 10/09): período 26/08→26/09 em US$ 52,62 gastos e US$ 94,96 estimados para o workspace; ChatND é US$ 38,60 dos 52,62; período anterior US$ 113,02. **Embedding não é o custo**: desde 03/09 foram 26.337 chunks (42,7 M chars ≈ 12 M tokens ≈ US$ 0,25 na OpenAI); o peso foi tempo/CPU — `RAG_EMBEDDING_BATCH_SIZE=1` (uma chamada por chunk) e cada arquivo da esteira embutido **duas vezes** (coleção `file-<id>` no upload + coleção da base ao vincular; `process_file` reaproveita o texto e recalcula o vetor). |
| D41 | **`MALLOC_ARENA_MAX=2` fica — é decisão, não efeito colateral de deploy** (10/09/2026) | Colocado no `Dockerfile` pelo Claude com o Davi em 06/09 (commit `3b51d7650`, **PR #65**, mergeado em 06/09 12:11 UTC), com runbook, prova em Docker e sondas em `_nidum_manutencao/`. **Só entrou em produção em 09/09 às 21:32 UTC**, no deploy que levou o #66 — não houve deploy entre 04/09 e 09/09, então os 10–17 GB dos dias 06–09 eram o container antigo. **Medição (10/09, `railway metrics --since 12h` + `/proc/1/status`):** 12 h com RAM plana em **1,58–1,59 GB**, 104 threads no PID 1, RSS do worker 1,2 GB — contra 14,6 GB médios/16,8 GB de pico nos 3 dias anteriores e 896 threads no diagnóstico de 06/09. Ao preço publicado do Railway (US$ 10/GB-mês de RAM), é a diferença entre ~US$ 146 e ~US$ 16 por mês só de memória — **o maior ganho de custo do projeto**. Fica no `Dockerfile` (versionado, revisável), não como variável do painel. O **passo 2** do runbook (`OMP_NUM_THREADS=8`) **perde o objeto** se o reranker sair do container (D42): as threads OpenMP vêm de torch/reranker e do Whisper — reavaliar depois da troca, medindo. |
| D42 | **Reranker FICA local por enquanto** (10/09/2026) — a troca por API foi avaliada e **adiada**; reabre quando alguém ler a política de retenção do provedor | A troca **compensa em custo**: `bge-reranker-base` local = 3,2 GB no volume + ~1,2 GB de RAM permanente + CPU em toda consulta híbrida (uma chamada por coleção consultada, ~96 candidatos, `TOP_K=48`, `TOP_K_RERANKER=50`), contra ~US$ 1 a 7/mês por API. Mas **adiciona um terceiro sub-processador recebendo trechos do acervo** (inclusive FONTE) — a 1.57.0 tirou a Azure justamente para não ampliar esse rol — e **a política de retenção de dados de Jina e Cohere não foi lida**. Sem a leitura, não se liga. Levantado, para quando reabrir: o Open WebUI 0.9.6 tem **um só motor externo** (`RAG_RERANKING_ENGINE=external`, `retrieval/models/external.py`: `POST` com `{model, query, documents, top_n}`, Bearer, lê `results[].index/relevance_score` — formato Jina/Cohere). Compatíveis sem shim: **Jina** (`https://api.jina.ai/v1/rerank`, `jina-reranker-v3`, US$ 0,05/M tokens, 10 M grátis) e **Cohere** (`https://api.cohere.com/v2/rerank`, `rerank-v3.5`, US$ 2/1.000 buscas). Voyage e Mixedbread devolvem `data[]` — exigem adaptador. Volume real (`chat_message`): 127 perguntas em 30 dias, pico 567 em julho. Qualidade: `bge-reranker-base` é de 2023, chinês/inglês, BEIR nDCG@10 51,6; `jina-reranker-v3` publica 61,7, multilíngue; Cohere 3.5 não publica BEIR comparável; sem benchmark público em português — **medir nos sete testes da Fase A antes e depois**. Configuração é **painel** (Admin → Documentos → Reranking: External, URL completa, chave, modelo), valor no banco (D24), sem reindexação, sem restart. Depois da troca o modelo sairia do volume e da RAM. |
| D43 | **Zerar a base de conhecimento está DESCARTADO** (10/09/2026) | A proposta resolveria o que **já foi resolvido** (RAM, via D41), o que **não está na base** (2.600 cópias locais órfãs no volume, 90 MB; 11.017 chunks de anexos de chat em coleções `file-*`, ~212 MB da tabela) e o que é **de código** (embedding duplo, D40). E **repetiria o pico da semana**: reindexar 731 arquivos = mais ~13 mil chunks × 2 e mais um dia de esteira. O que a base tem de custo real se resolve por partes menores e reversíveis: lote de embedding (1 → 64, painel), limpeza com simulação (`_nidum_manutencao/limpeza_armazenamento.py`, D27) e retenção. |
| D44 | **Aplicar pega o que escrever nao pega — os dois defeitos da ficha so apareceram na hora de gravar** (10/09/2026) — pedido do Davi como registro proprio | **Os dois casos, e nenhum foi pego por leitura, teste ou redacao do PR.** (1) **Dupla extensao:** o gerador montava `ata.md` + `.fatos.md` = `ata.md.fatos.md`. Invisivel no codigo (`caminho + SUFIXO` esta certo), invisivel no teste (que provava o CONTEUDO da ficha, nao o nome do arquivo), invisivel no desenho (que escrevia `ATA_Comite.fatos.md` e portanto ja estava certo no papel). Apareceu no `ls`. **E nao e cosmetico:** o nome do arquivo vira a CHAVE na colecao — `coletar_md` troca `/` por ` > ` — entao e o que o agente le na listagem, repetido 56 vezes. (2) **Idempotencia:** a ficha mora na pasta de atas, logo o criterio da uniao a reconheceria como ata, e a rodada seguinte geraria `.fatos.fatos.md`, e a de depois `.fatos.fatos.fatos.md`. So apareceu ao rerodar o gerador **depois** de gravar — a primeira execucao de um gerador nunca mostra o que a segunda faz. Consertado com `tipo: ficha` no carimbo (a raiz) mais o sufixo no nome (o cinto). **O que os dois tem em comum, e e o registro:** ambos sao propriedades do ARTEFATO no mundo — o nome que ele ocupa, o estado que ele deixa para a proxima rodada — e nao do codigo que o produz. Teste unitario prova a funcao; leitura prova a intencao; escrever o PR prova o raciocinio (D38). **Nenhum dos tres olha para o que fica no disco depois.** **A regra operavel:** todo gerador que escreve arquivo se roda DUAS VEZES antes do merge, e se olha a SAIDA (o `ls`, o nome, a segunda rodada), nao so o retorno da funcao. **Familia:** fecha com D26 (teste pega o que leitura nao pega), D37 (dado real pega o que teste nao pega) e D38 (escrever pega o que rodar nao pega) — este diz que **aplicar pega o que escrever nao pega**, porque escrever prova o que deveria acontecer e aplicar mostra o que fica. **Nota de numeracao:** o Davi pediu "D38"; o D38 ja existe (*Escrever a prova e a ultima conferencia*) e D40-D43 estao no PR #68 ainda aberto, entao este entrou como D44. |
| D45 | **Item numerado citado sem fonte vira folclore — a "regra 7" que travou o gerador de atas nao existia, e colidia com a regra 7 de verdade** (10/09/2026) — correcao do Davi sobre erro proprio | **O caso.** Uma "regra 7" foi invocada para barrar mexer no gerador de atas: *"Nao alterar as atas ja geradas. Nao mexer no gerador de atas (repo GeradorAta)"*. Varredura completa das cinco frentes, dos `.claude` e do historico do git dos tres repos: **ela nao esta em repositorio nenhum**. Estava no `claude_TEC_PromptMestre_ChatND_CamadaDeFatos_18082026_v1.md`, item 7 — e os HTMLs da mesma data dao o contexto que o item perdeu: era **escopo de fase**, nao proibicao permanente. A camada de fatos, como desenhada em agosto, *so lia*. **O erro foi meu** (Davi): copiei para o prompt-mestre de setembro como regra inviolavel, e ela **perdeu o prazo**. **Duas consequencias, e a segunda e pior que a primeira.** (1) A regra venceu e continuou valendo — um escopo de fase virou proibicao vitalicia sem que ninguem tivesse decidido isso. (2) **Colidiu com a regra 7 REAL**, do `05_Guia_do_Contribuidor.md`, que e *"Nao renomear um modelo base na UI achando que cria um wrapper"* — de modo que "regra 7" passou a apontar para duas coisas diferentes conforme quem cita. **Por que isto e D33 aplicado a regra:** o D33 diz que afirmacao sem manutencao vira folclore; aqui a afirmacao carregava um NUMERO, e numero da aparencia de fonte. "Regra 7" soa verificavel — e ninguem verifica, justamente porque soa. **A regra que fica:** regra citada por numero se cita **com o arquivo**; regra que nasce de escopo de fase leva **a fase no texto** ("enquanto a camada de fatos so ler"), para vencer sozinha em vez de sobreviver por inercia. E prompt-mestre **nao e onde regra permanente mora** — ele copia, e copia nao envelhece junto com o original. **O desfecho:** a protecao real do gerador nunca foi essa regra — e a **idempotencia** (`a saida gravada e o .pdf; o gerador pula o que ja existe, nunca reprocessa nem sobrescreve`), escrita no `CLAUDE.md` do proprio `gerador-atas`. Mudar o formato de saida nao toca em nada ja gerado, e por isso as duas linhas do `src/ata.py` foram autorizadas. **A conclusao estava certa; a fonte, nao.** |
| D46 | **Duas camadas de exclusao, uma conferencia que so conhecia a primeira** (10/09/2026) | **O numero:** 70 dos 147 "faltantes" da conferencia diaria eram arquivos que o sync foi **instruido a nunca colocar na base** — due diligence, NF de coautor, `Chico/1 - Cadastros`, certidao com CPF, stub vazio. A conferencia os esperava la e chamava a ausencia de **"SUMICO INEXPLICADO"**. **A causa, e ela e estrutural e nao um descuido:** existem DUAS camadas de exclusao e elas moram em modulos diferentes. O **puxador** (`regras_governanca`) decide o que entra no **repo** — escopo, obsoleto, subpasta excluida, tipo/tamanho. O **destino** (`colecao_destino.excluir`) decide o que entra na **base** — dado pessoal, stub. `montar_esperado` espelhava so o primeiro, deliberadamente, porque foi escrito como espelho do ramo *"em escopo e VIGENTE"* do `classificar_mudancas`. Estava fiel ao que copiou; o que copiou e que era metade. **Por que ninguem via:** o alarme era diario, tinha 145 itens, e **estava certo sobre 75 deles** — um alarme majoritariamente falso nao parece quebrado, parece barulhento, e barulho se aprende a pular. **O conserto e no conferidor, e nao no sync:** o sync estava certo. `excluir` foi partida em `excluir_por_caminho` mais o resto, e a conferencia chama a primeira — fonte unica, sem copia (Regra 3). As regras que dependem do CORPO seguem invisiveis para ela, e isso e honesto: nao sao conheciveis sem o arquivo, e a Regra 2 diz que ela le o SharePoint, nao o repo. **A regra que fica:** toda conferencia que reconstroi um estado esperado precisa declarar **quais camadas de decisao ela reproduz** — e uma que reproduz um subconjunto nao esta incompleta, esta **errada**, porque a diferenca sai como acusacao. |
| D47 | **Artefato gerado vira orfao permanente numa conferencia de espelho — e orfao e a categoria que manda apagar** (10/09/2026) — defeito criado no mesmo dia pela Fase D | **O caso:** as 56 fichas de fatos (`.fatos.md`) sao **geradas** a partir das atas. Nao existem no SharePoint e nunca vao existir. A conferencia diaria compara a base com o SharePoint, entao no dia seguinte ao merge apareceram **56 orfaos**. **A consequencia e a pior possivel: um alarme diario que, se alguem obedecer sem ler, apaga a Fase D.** (Palavras do Davi.) Orfao nao e uma categoria informativa — e a unica cuja reconciliacao prescrita e **remover da base**. **Por que e diferente do D46:** la a conferencia esperava a mais e acusava ausencia; aqui ela **encontra a mais** e acusa excesso — e a acao sugerida para excesso e destrutiva. Sumico manda investigar; sobra manda apagar. **A assimetria importa:** um alarme falso de faltante custa atencao; um alarme falso de orfao custa o acervo. **O criterio:** a ficha e legitima **enquanto a ata que a originou for esperada**. Se a ata saiu do SharePoint, a ficha vira orfa de verdade e sai junto — senao o acervo passa a servir fatos de um documento que nao existe mais. As duas metades sao testadas; sem a segunda, a primeira seria cheque em branco. **A regra que fica:** todo artefato **derivado** que entra na base precisa ensinar a conferencia a reconhece-lo **no mesmo PR que o cria** — e nao no dia seguinte, quando o alarme ja disparou. A Fase D nao fez isso, e foi sorte o alarme ter sido lido antes de obedecido. |
| D48 | **Guarda que existe, funciona, e vigia o lugar errado — 67 arquivos perdidos em silencio na area de passagem** (10/09/2026) — formulacao do Davi, o achado do dia | **O caso.** O puxador baixava todo item do SharePoint para `_entrada/<nome do arquivo>`. Dois arquivos de mesmo nome em pastas DIFERENTES do SharePoint escreviam no MESMO caminho e se sobrescreviam — junto com o bilhete `.meta.json`, que e o que diz de onde cada um veio. Sobrava um, convertido com o metadado do ULTIMO. `TUTORIAL.txt` existe **24 vezes** no acervo e sobreviveu **uma**. Ao todo, **67 arquivos** perdidos assim. **E havia uma guarda de colisao. Ela funcionava. E olhava para o outro lado.** `relato_colisoes` compara o **`.md` PREVISTO** de cada item — e dois `TUTORIAL.txt` em pastas diferentes preveem caminhos diferentes, porque a pasta entra na chave. Para ela nao havia colisao nenhuma, e ela estava certa: **a colisao nao era no destino, era na PASSAGEM**. **Por que isto e uma familia nova, e nao mais um caso do D26/D37/D38/D44:** nos anteriores, o mecanismo de protecao estava ausente, desligado por um modo, ou cego por comparar zero com zero. Aqui ele esta **presente, ligado, correto e exercitado** — e mesmo assim nao cobre o evento, porque foi apontado para o estagio errado do pipeline. Nenhuma revisao de codigo pega isso: a guarda passa em qualquer leitura, porque o defeito nao esta nela. **O sintoma era mudo.** O estado do delta registrava os 24 como gravados; a quarentena ficava vazia; nenhum log reclamava. O unico sinal era a conferencia diaria dizendo **"SUMICO INEXPLICADO"** um dia depois — e dizendo certo. **A regra que fica:** uma guarda protege **um estagio**, e nao um processo. Para cada guarda, escrever **em que estagio ela olha** e **quais estagios ficam sem guarda nenhuma** — e a area de PASSAGEM (buffer, staging, fila, temporario) e onde ninguem olha por padrao, justamente porque ela "nao guarda nada". **Corolario pratico:** todo lugar onde um identificador do mundo externo vira **nome de arquivo local** e um ponto de colisao, e a chave usada la precisa ser tao unica quanto a chave do destino. Aqui o conserto foi uma subpasta por `id` do driveItem. |
| D49 | **Testes 2 e 3 da Fase A: a linha de base de 10/09, com a evidencia** (10/09/2026) | **Teste 2 - o `#` escopa as ferramentas: PASSOU.** A evidencia e `knowledge_ids: ["52c794ea-caa8-4f58-a343-d08a79f8110c"]` na chamada de `query_knowledge_files` - UM id, o de Reunioes, e so ele. Antes do PR #64 a mesma pergunta chamava a ferramenta **sem** `knowledge_ids` e trazia fontes de Gestao de Projetos e ACA, bases que ninguem selecionou. O chip aparecia na mensagem e o contexto era injetado; a ferramenta e que ficava desescopada, e o modelo decide pela ferramenta. **Teste 3 - enumeracao de atas: 6 atas, contra 4 antes.** Melhorou e continua sendo top-k: `query_knowledge_files` devolve os k trechos mais parecidos, nunca todos. **O 6 e a linha de base NOVA** - e o numero contra o qual a Fase D tem de ser medida, e nao o 4 da Fase A. **Teste 1 - listagem de bases: FALHOU na medicao de 05/09** e foi reclassificado: nao era regressao de raciocinio, era bug de listagem (PR #66, `count=10` num universo de doze mais ordenacao por `updated_at`). **Por que registrar numero de teste e nao so o veredito:** o 4 virou 6 sem que nada no raciocinio mudasse - mudou o alcance. Sem o numero anotado, a proxima medicao compara com a lembranca, e lembranca de numero e sempre generosa. |
| D50 | **Duplicata entre pastas do SharePoint e a pasta REFLETIDA, nao defeito — os 42 grupos anteriores a 05/09 ficam** (10/09/2026) — regra do Davi, para ninguem "consertar" depois | **O caso:** a investigacao do crescimento de +285 arquivos achou 123 grupos de conteudo duplicado no repo. **81 eram mistos** (um lado velho, um lado novo) e vieram dos resyncs de 05/09 - esses sairam no PR #182. **Os outros 42 sao anteriores e FICAM.** **A razao, e ela e de desenho e nao de tolerancia:** desde o D32 a base espelha a PASTA. Se o mesmo documento existe em duas pastas do SharePoint, ele existe em duas bases - e isso nao e erro de roteamento, e o espelho funcionando. Deduplicar por conteudo quebraria a premissa: quem seleciona `Operacoes` receberia um documento a menos porque alguem, um dia, guardou uma copia dele em `Produtos`. **A duplicata e um fato do SharePoint, e a base o reflete.** **Irmao do D36**, que diz a mesma coisa sobre `Plataformas Regionais`: *o unico arquivo atual continua sendo copia do que esta em Tecnologia, e isso e a pasta refletida, nao defeito*. **O que fica proibido:** deduplicacao automatica por hash de conteudo entre bases. Se a duplicata incomodar, o conserto e no SharePoint - apagar ou mover a copia la -, nunca na esteira. **O que continua valendo:** duplicata dentro da MESMA pasta, e sobra de caminho abandonado (id nenhum reivindica), sao defeito e saem. |
| D51 | **A comparacao que nunca acontece: objeto que nao esta em nenhuma das duas listas nao e conferido por nenhuma das duas conferencias** (10/09/2026) | **O caso:** a colecao `Projetos` (a antiga `nd-projetos`, 17 arquivos) sobreviveu ao passo 9 da migracao e continuou VIVA no painel - acessivel ao agente, aparecendo na listagem de bases, competindo na busca. **E nenhum relatorio a via.** O de orfaos compara o `sync_config` com o repo, e ela nao estava no config; a classe `base_indevida` compara as pastas-mae DECLARADAS como excluidas, e ela nao era uma delas. Ficava exatamente no vao entre os dois: **invisivel para quem confere e visivel para quem pergunta.** **Por que e distinto do D37:** la a comparacao ACONTECIA e dava zero contra zero; aqui ela **nao acontece** - nao ha par a comparar, porque o objeto nao pertence a nenhum dos conjuntos que alguem decidiu cruzar. Uma e cegueira; a outra e ausencia de olho. **A regra que fica:** para cada conferencia, perguntar **qual e o universo** que ela percorre - e se esse universo e uma lista DECLARADA (config, escopo, inventario), entao tudo que existe fora dela e invisivel por construcao. A cobertura de uma conferencia nao e o que ela compara; e o que ela ENUMERA. |
| D52 | **O modo seguro desliga a protecao — os seis casos, e a forma que eles tem em comum** (10/09/2026) — pedido do Davi como classe | **Seis casos, e cinco deles no mesmo dia:** (1) **`resync` desliga a deteccao de movimentacao** - `inicial = (delta_link is None) or resync`, e a reconciliacao esta atras de `if not inicial`. Dois resyncs deixaram 92 caminhos abandonados (PR #183). (2) **`dry_run` fechava a issue de bloqueio** - simulacao prudente pagava divida que nao pagou. (3) **`padrao` roteava sem avisar** - o catch-all mandava para a maior colecao o que ninguem decidiu onde vai. (4) **Full Context quebrava as ferramentas.** (5) **`STORAGE_LOCAL_CACHE=true`, o DEFAULT, desliga a limpeza da copia local** - a funcao existe, esta correta, e chamada, e nao faz nada (PR #72). (6) **`--confirmar-remocao-em-massa` NAO vence a catastrofe** - a checagem vem antes do `if confirmado`, entao a via de excecao consciente nao abre a porta que ela parece abrir. **A FORMA COMUM:** o mecanismo EXISTE, esta CORRETO e e EXERCITADO - e um modo, uma flag ou um default o torna inerte, sem log, sem aviso e sem teste vermelho. Nenhuma revisao de codigo pega, porque **o codigo esta certo**: o que esta errado e o alcance. **O parente proximo e o D48**, e o eixo e diferente: la a guarda vigia o ESTAGIO errado (ou a CATEGORIA DE ERRO vizinha); aqui ela vigia o lugar certo e esta **desligada**. **A regra que fica:** todo modo, flag e default que altera comportamento de protecao entra num inventario escrito - **qual protecao ele desliga** -, e todo teste de protecao ganha um caso NO MODO que a desliga. Um teste que so exercita o caminho normal prova a protecao que ninguem desliga. |
| D53 | **Alarme certo numa sala vazia: a conferencia gritou 86 orfaos por dias e ninguem leu** (10/09/2026) | **O caso:** a conferencia diaria vinha reportando **86 orfaos e 147 faltantes** na issue #128, acumulando um comentario por dia desde 09/09. O numero estava CERTO - eram exatamente os 86 que a convergencia removeu. O instrumento funcionava, media bem e falava todo dia. **O que faltou nao foi instrumento: foi alguem lendo o numero.** **Por que nao e o mesmo que os outros registros:** no D48 a guarda olha o lugar errado; no D52 ela esta desligada; no D37 ela compara zero com zero. Aqui ela esta **certa, ligada e correta** - e a falha e do lado humano do alarme. **A causa real, e ela e de desenho:** um alarme DIARIO com **145 itens** dos quais **75 eram explicados** nao parece quebrado, parece **barulhento** - e barulho se aprende a pular. Alarme majoritariamente falso treina o leitor a nao ler, e depois disso o alarme verdadeiro chega no mesmo envelope. **O conserto nao foi pedir mais disciplina:** foi tirar os 70 falsos (PRs #187 e #189) e dar ao resto uma lista com MOTIVO por linha, ordenada com os inexplicados no topo. **A regra que fica:** a taxa de falso positivo de um alarme e parte do alarme. Um relatorio que acusa 145 e acerta 70 nao esta "70% certo" - ele esta treinando alguem a ignorar os 70. |
| D54 | **Conteudo que parece conteudo e pior que ausencia, porque a ausencia a esteira declara** (10/09/2026) — principio unico, pedido do Davi | **De onde vem:** a formulacao nasceu na ficha da Fase D, para decidir o que fazer com a secao de pendencias ilegivel - e vale para toda a esteira. **Os casos que ela explica, e sao de origens diferentes:** (a) **tabela de pendencias extraida celula a celula** produzia 2.296 "fatos", entre eles `Tarefa`, `Responsavel` e `Prazo`, que sao o CABECALHO. Um agente responderia que existe uma pendencia chamada "Prazo". (b) **`.md` truncado a 80%** tem cabecalho valido, passa pela guarda de vazios e e publicado na base como verdade. (c) **`.pdf` pela metade** converte para lixo plausivel. (d) **livro-caixa de quarentena truncado** le como VAZIO e a proxima gravacao escreve o vazio por cima. (e) **stub vazio** - 39 PDFs sem texto extraivel - que o destino barra justamente por nao ter conteudo. **A assimetria:** a ausencia a esteira DECLARA - quarentena, faltante, cobertura=ilegivel, `sem_estrutura`. O meio-conteudo ninguem declara, porque ninguem sabe que ele e meio. Ele atravessa todas as guardas parecendo inteiro, e o unico modo de flagra-lo e alguem estranhar a resposta. **As tres consequencias de desenho:** (1) escrita de artefato final e ATOMICA - temporario + rename (PR #192); (2) extracao que nao tem certeza NAO produz saida parcial: declara `ilegivel` e manda ler a origem; (3) guarda de tamanho minimo nao substitui nenhuma das duas, porque truncamento a 80% passa por ela. |
| D55 | **39 PDFs sem texto extraivel: categoria conhecida, decisao de origem** (10/09/2026) | **O numero:** 42 `.md` no repo tem corpo vazio ou quase. **39 sao PDFs dos quais a conversao nao extraiu NENHUM caractere** - apresentacoes exportadas como imagem ou documentos escaneados. Os outros 3 nao sao essa categoria e foram conferidos um a um: um `.docx` chamado `Documento.docx` genuinamente vazio, um `.html` de assets de pagina salva, e um `.html` que contem so `nidum / Unpacking...` (shell de aplicacao JS). **O que ja funciona:** o `MIN_CHARS_PDF = 200` do converter nomeia o caso (*provavelmente escaneado - revisar manualmente; talvez precise de OCR na origem*), e o destino os barra como `stub vazio`, entao **eles nao estao na base**. Nao ha vazamento e nao ha defeito de esteira. **Por que nao e conserto nosso:** OCR e transformacao do ORIGINAL. Fazer na esteira significaria a base servir um texto que o documento no SharePoint nao tem - duas versoes da mesma verdade, e a nossa sem revisao humana. A correcao certa e no SharePoint. **Lista nominal dos 39** entregue ao Davi em 10/09/2026, para levar a quem cuida do SharePoint. **ENCERRADO EM 12/09/2026 — e NAO vira acao na origem** (decisao do Davi, depois de ver a lista): *sao apresentacoes exportadas como imagem. Alguns PDFs sao so imagem mesmo, e nao ha o que extrair.* **A reclassificacao e o ponto deste encerramento:** isto deixa de ser pendencia e passa a ser **estado conhecido do acervo**. Nao e defeito da esteira (ela nomeia o caso e barra o stub), nao e defeito da origem (o documento e uma imagem porque foi feito assim), e nao ha conserto - nem nosso nem de quem cuida do SharePoint. **POR QUE A DISTINCAO IMPORTA:** pendencia aberta pede acao toda vez que alguem le a lista, e uma que nunca sai vira ruido - o mesmo mecanismo que faz alarme cronico ser desligado (D53). Estado conhecido se le uma vez e explica o numero para sempre. **O que fica para quem reencontrar os 39:** a resposta ja esta dada, o carimbo esta aqui, e reabrir exige fato novo - nao o reencontro do mesmo numero. |
| D56 | **Basename nao identifica documento neste acervo — e todo lugar que o usa como chave e um defeito esperando** (11/09/2026) — formulacao do Davi, depois de o mesmo erro aparecer em QUATRO camadas no mesmo dia | **As quatro camadas, e elas sao independentes entre si:** (1) **PASSAGEM** — o puxador baixava para `_entrada/<nome do arquivo>`, e dois arquivos homonimos de pastas diferentes se sobrescreviam junto com o bilhete. `TUTORIAL.txt` existe **24 vezes** e sobreviveu **uma**; ao todo **67 arquivos** perdidos (#188). (2) **PAREAMENTO** — o sync procurava o "legado" de um documento movido por nome de arquivo e pegava `cand[0]`, ARBITRARIO entre homonimos: com 24 `TUTORIAL.md` na mesma colecao, mover um poderia sobrescrever o de outro projeto, sem erro e com a contagem fechando (#198). (3) **CHAVE** — `md_previsto` deriva o `.md` do *stem*, entao o mesmo documento em `.pdf` e `.pptx` disputa o mesmo arquivo: **54 colisoes**, resolvidas por "o ultimo processado" (#197). (4) **IRMAO** — a primeira versao do `caminhos_abandonados` casava por basename e emparelhou `RTF-005-Meraki/.../DUO DILIGENCE/x.md` com `RTF-009-Artefacto/.../x.md`, **ativos de retrofit diferentes**; pegou no proprio dry-run e o criterio virou corpo identico. **POR QUE ESTE ACERVO EM ESPECIAL:** a arvore do SharePoint repete o mesmo nome por PROJETO — `TUTORIAL.txt`, `CHECKLIST_IPANEMA.pdf`, `ND-01_plantas_polycam.pdf`, `FONTE.txt`. O nome descreve o PAPEL do documento dentro da pasta, e nao o documento. **A pasta e que identifica, e por isso o eixo por pasta (D32) esta certo pelo mesmo motivo que este registro existe.** **A varredura pelo quarto lugar (pedida pelo Davi) fechou:** os demais usos de `os.path.basename` sao legitimos — derivar *stem* para o carimbo, montar chave de backup, ler o bilhete ao lado do bruto. E no `nidum-platform` o armazenamento ja prefixa com `uuid` (`{id}_{filename}`), entao la o basename E unico por construcao. **A regra que fica:** basename so serve como chave onde alguem GARANTE unicidade — e a garantia tem de estar escrita no ponto de uso, nao suposta. Onde nao houver, use o `id` da origem, o caminho inteiro, ou o corpo. |
| D57 | **A barra final: a forma mais barata do defeito caro** (11/09/2026) — palavras do Davi | **O caso.** `modelo_renomeado` foi a unica classe NAO CONFERIDA do relatorio por dias, com a mensagem *"a base nao respondeu a /api/v1/models/ (Expecting value: line 1 column 1 (char 0))"*. A leitura natural e credencial ou rede — e as duas estavam certas. **A causa estava escrita no proprio backend, na linha de cima do endpoint:** `@router.get('/list', ...)  # do NOT use "/" as path, conflicts with main.py`. O router **nao define `/` de proposito**, porque `main.py` registra `@app.get('/api/v1/models')` (sem barra) como compatibilidade com a API da OpenAI. Pedir COM barra nao casa rota nenhuma no router, cai no redirecionamento de barra do FastAPI e volta sem corpo JSON. **Um caractere.** **Por que e a forma mais barata do defeito caro:** o conserto foi trivial e o custo foi dias de uma conferencia cega — e o aviso ja existia, escrito, **do outro lado do repositorio**. Nao faltava conhecimento: faltava o caminho entre o sintoma e o lugar onde a explicacao morava. **A regra que fica:** quando um endpoint responde algo que nao e o esperado, LER A DEFINICAO DA ROTA antes de suspeitar de credencial, rede ou permissao — essas tres sao as hipoteses caras, e a definicao da rota e a barata. E quando existir um comentario avisando de uma armadilha, ele merece virar TESTE: o comentario salvou quem escreveu o backend e nao salvou quem escreveu o cliente. **Familia:** irmao do D48 — la a guarda vigiava o lugar errado; aqui a informacao estava no lugar certo e ninguem passou por ele. |
| D58 | **O modo seguro que ninguem escolheu: `STORAGE_LOCAL_CACHE` vem `true` de fabrica e desliga a limpeza** (11/09/2026) — a sexta ocorrencia da classe do D52, e a pior | **O caso.** `_cleanup_local_cache` existe, esta correta, tem a guarda certa, e e chamada. E comeca assim: `if STORAGE_LOCAL_CACHE or STORAGE_PROVIDER == 'local': return`. `STORAGE_LOCAL_CACHE` tem default `'true'`. **A limpeza existia, era chamada, e nao fazia nada** — e a copia local de todo arquivo ficava no volume do Railway para sempre, mesmo com o R2 ja tendo o arquivo. **POR QUE E PIOR QUE AS OUTRAS CINCO, e e a razao de ter registro proprio:** nos casos do D52 alguem ESCOLHEU o modo — rodou `--resync`, marcou `dry_run`, ligou o Full Context. A escolha foi consciente e o efeito colateral e que passou despercebido. **Aqui ninguem escolheu nada.** O sistema veio assim, e o estado padrao e o estado em que quase todo sistema fica — default e o que vale para quem nunca olhou. Um modo que alguem liga tem, em algum lugar, uma pessoa que sabe que o ligou; um default nao tem ninguem. **A regra que fica:** o default de toda variavel que governa protecao, limpeza ou freio entra no mesmo inventario escrito do D52 — e a pergunta a fazer na revisao nao e "o que este modo faz quando ligado?", e **"o que este codigo faz com os valores que ninguem configurou?"**. **Consequencia pratica:** o PR #72 acrescenta as chamadas que faltavam (upload ao S3, loader do retrieval, download, transcricao), mas o codigo sozinho nao muda nada — ligar exige `STORAGE_LOCAL_CACHE=false` no painel, e isso e decisao de operacao. |
| D59 | **A LINHA DE BASE DO PRODUTO: 20 origens, e as quatro medicoes lado a lado** (11/09/2026) — decisao do Davi de fixar o numero que mede qualquer mudanca futura | **As quatro medicoes, mesma pergunta, mesmo acervo, arquiteturas diferentes:** semana passada **2** origens · Fase A, 03/09 **4** (quatro atas de 108; ~96% do acervo invisivel) · 10/09 **6** (listagem e `#` consertados) · **11/09 — 20 origens, 8 delas `.fatos.md`, cruzando 7 pastas** (Reunioes, MUN, TEC, PROD, MKT, Juridico/Contratos), com 21 linhas de pendencia organizadas por origem em HTML on-brand. **O QUE O NUMERO MEDE, e nao e raciocinio:** e ALCANCE. Em nenhuma das quatro o agente inventou, fingiu completude ou contradisse a fonte - em todas ele respondeu com honestidade sobre o pedaco do acervo que conseguia ver. O que mudou foi o tamanho do pedaco. **A EVIDENCIA DE QUE A FASE D FUNCIONA COMO DESENHADA:** o agente foi **a ficha primeiro e a ata onde precisou** - filtrou antes de ler, que era exatamente a premissa (*"a ficha nao existe para ser lida inteira; existe para ser filtrada antes de lida"*). E no teste 2 uma ficha apareceu nas citacoes de uma resposta real, entregando justamente o item que as atas nao davam limpo. **POR QUE FIXAR ISTO COMO REGUA:** sem o numero anotado, a proxima medicao compara com a lembranca - e lembranca de numero e sempre generosa (foi o que quase aconteceu quando o 4 da Fase A virou referencia informal). **A comparacao futura e contra 20, nunca contra 6 ou 4.** E a regua so vale com a base fechada: 759 arquivos, onze bases, 0 orfaos, 0/0 por colecao - *regua contaminada e numero sem metodo*. |
| D60 | **O que a E1 NAO entregou: 31 das 56 fichas com pendencias ilegiveis — e a medicao que decide regerar** (11/09/2026) | **O numero, dito sem suavizar:** das 56 fichas geradas, **31 tem a secao de pendencias marcada `cobertura: pendencias=ilegivel`**. A causa nao esta na extracao: esta na ORIGEM. O gerador de atas pedia *"Tabela com colunas: Tarefa | Responsavel | Prazo"*, e a tabela sai correta no PDF mas a conversao para `.md` a parte - tarefa numa linha, responsavel noutra, cabecalho virando conteudo. **E a pergunta do teste 1 era sobre PENDENCIAS**, que e justamente onde a cobertura e pior. O 20 do D59 e real; nao e completo. **O QUE JA FOI FEITO:** GeradorAta #9 trocou duas linhas do prompt - a secao passa a sair em LISTA (`- <tarefa> | resp: <x> | prazo: <y>`), formato que o extrator ja le limpo nas Decisoes da mesma ata. Conserta na origem, para toda ata futura. **A IDEMPOTENCIA E O QUE TORNA ISSO SEGURO:** o gerador PULA o que ja existe - nunca reprocessa nem sobrescreve -, entao **nenhuma ata ja gerada foi tocada**. As 31 antigas ficam. **A MEDICAO QUE DECIDE (pedida pelo Davi):** quando houver a primeira ata nova no formato lista, comparar a ficha dela com a de uma ata antiga equivalente - quantos fatos, quantos com responsavel e prazo, e se a cobertura sai `lida`. **E esse delta, e nao a intuicao, que decide se vale regerar as 31.** Regerar e decisao separada e nao esta tomada. |
| D61 | **ACHADO: `base_model_id` viaja no payload de `/api/models` para QUALQUER usuario autenticado — a regra do modelo invisivel ja esta sendo violada por um caminho que ninguem olhou** (11/09/2026) — pergunta do Davi ao revisar o item #2 da Fase E | **O que o codigo diz, verificado linha a linha.** `@app.get('/api/models')` e `@app.get('/api/v1/models')` sao **o mesmo handler**, e ele pede `Depends(get_verified_user)` — *qualquer usuario logado*, nao admin. Para cada wrapper, `utils/models.py` monta `model['info'] = custom_model.model_dump()`, e `ModelModel` tem **`base_model_id`**. O handler remove so `profile_image_url` (e `params` foi apagado antes); `get_filtered_models` filtra QUAIS modelos aparecem, nao QUAIS CAMPOS. Junto vai `owned_by`, herdado do modelo base. **Logo: o id do modelo real, por tras de cada wrapper, esta exposto a toda a casa.** **E nao exige pericia:** o proprio frontend chama esse endpoint, entao o payload ja esta no navegador - devtools, aba Rede, `/api/models`. **POR QUE ISTO E ACHADO E NAO PENDENCIA DA FASE E:** a discussao comecou sobre se o preset agentico conseguiria esconder o modelo base sem wrapper. A resposta e que **os wrappers de hoje tambem nao escondem** - eles escondem do SELETOR, que e onde as pessoas olham, e nao da API, que e de onde qualquer um pode ler. Esconder da vista e esconder de quem procura sao barras diferentes, e a regra escrita (*nunca revelar qual LLM/provedor esta por tras*) nao distingue as duas. **O que a Fase E muda aqui: NADA.** O caminho ja esta aberto, e fecha-lo (ou aceita-lo) e decisao de hoje, nao do corte. **CONFERIDO EM PRODUCAO E ENCERRADO (11/09/2026).** O Davi testou pelo devtools com a propria conta: *Nidum 1.0 - Documentos*, *Nidum 1.0 - Geral*, *Chico* e *Identificador de Ambientes* mostram `claude-sonnet` e `claude-opus` no payload de `/api/models`. A leitura do codigo estava certa. **PRECISAO QUE MUDA A CONCLUSAO: vaza o MODELO, nao a CHAVE** - nenhuma credencial aparece. **DECISAO: nao e problema, nao consertar.** Todo mundo com conta e de dentro, e a regra protege o TEXTO das respostas (o ChatND nao diz "eu sou o Claude" quando perguntam), nao o payload de uma API interna. A regra foi reescrita com esse escopo explicito no `CLAUDE.md`. **ISTO E CASO RESOLVIDO, NAO INCIDENTE** - e a distincao importa para quem ler depois: nada foi consertado porque nada estava quebrado. **A LICAO DE METODO, que e o que vale guardar (palavras do Davi):** o item #2 da Fase E estava na coluna de BLOQUEADOR por causa de uma regra; investigar revelou que o bloqueador era IMAGINADO e que havia uma violacao REAL ao lado, invisivel; e a violacao, examinada, NAO era violacao - **a regra e que estava mal escrita**. Tres camadas, e nenhuma era o que parecia. O padrao a levar: quando um item parece bloqueado por uma regra, conferir nesta ordem - *o mecanismo e mesmo assim?*, *a regra diz mesmo isso?*, *a regra esta escrita com escopo?*. Parar na primeira camada teria adiado o corte por nada; parar na segunda teria aberto um incidente por nada. |
| D62 | **`ENABLE_PERSISTENT_CONFIG` FICA como esta (`True`) — e o pre-requisito para um dia mexer esta escrito** (11/09/2026) — decisao do Davi | **O que ele faz:** com `True` (default), a configuracao vem do **banco** e a variavel de ambiente so vale na primeira vez. O codigo e explicito: `if self.config_value is not None and _persist_enabled: self.value = self.config_value`. **O sintoma que produz e o pior do conjunto:** muda-se a variavel no Railway, o deploy sobe **verde**, e o comportamento **nao muda**. Nada erra - so nao obedece. E a causa ja observada do `RAG_EMBEDDING_BATCH_SIZE=1` persistir apesar do ambiente, e e o D24 em forma de infraestrutura: configuracao mora no banco, e o banco nao deixa rastro em commit. **A DECISAO, com as palavras do Davi:** *trocar a fonte da verdade de todas as chaves de uma vez, num restart, sem lista previa do que muda, e exatamente o tipo de operacao que a semana ensinou a nao fazer.* Fica `True`. **O PRE-REQUISITO, e ele e a parte operavel deste registro:** se um dia for necessario desligar, o passo anterior e o **inventario completo de banco x ambiente** - quais chaves divergem e para que valor cada uma iria. Hoje ninguem tem essa lista, e sem ela desligar e uma mudanca de N comportamentos com N desconhecido. **E o caminho barato continua aberto:** para fazer uma mudanca valer, altere o valor **no painel**, que e onde a verdade mora. |
| D63 | **O publish sai da maquina de alguem e passa a sair do repositorio — com simulacao obrigatoria por padrao** (11/09/2026) — decisao do Davi, recusando a trava B como estava proposta | **O que motivou.** A conferencia de 11/09 achou `chatnd` 1.65.0 no painel x 1.65.0 no repo com **1.148 linhas realmente diferentes**, e o gerador 2.8.2 x 2.8.2 com **543** — e, procurando no **historico inteiro**, o conteudo publicado nao correspondia a **nenhum commit que ja existiu**. O publicador foi descartado como causa (manda o arquivo verbatim). Sobrou: *o que roda nao era rastreavel ao repositorio*. **A TRAVA B FOI RECUSADA, e a recusa e o registro que vale** (palavras do Davi): *recusar arvore suja e gravar o sha e um paliativo elegante para um problema que e outro: o publish e manual e local. Enquanto for alguem rodando comando na propria maquina, o rastro depende de disciplina — e a REGRA DA DOC diz que instrucao errada nao se corrige com esforco.* A B pedia disciplina com mais passos; nao trocava a instrucao. **O que entra no lugar:** publicar pelo GitHub Actions. O codigo sobe do proprio repositorio, *arvore suja deixa de existir por construcao*, o sha vem de graca e o token e secret do repo em vez de ficar com uma pessoa. Mesmo padrao que a esteira ja usa. **O que barateou a conta, e e o tipo de achado que muda uma decisao:** os secrets **ja estavam la** (`OPENWEBUI_BASE_URL`/`OPENWEBUI_API_KEY`, desde 05/09) e **ja sao de admin** — o conferidor le `content` de function com eles, e esse endpoint e admin-only. **Zero segredo novo.** **Confirmacao manual, sem publish automatico:** unico gatilho e `workflow_dispatch`, `dry_run` **ligado por padrao**, **um alvo por rodada** (sem opcao *todos*: triplicar o raio de alcance de um engano para economizar dois cliques e troca ruim). **O carimbo vai no `meta.description`, nunca no `content`** — o conferidor compara o `content` com o do repo, e carimbar dentro dele faria **todo** publicado divergir por causa do proprio carimbo. Publish local continua possivel e passa a sair carimbado como **LOCAL, sem sha**: carimbo que omitisse a origem local seria pior que carimbo nenhum. **O QUE ISTO NAO FAZ, dito para nao ser vendido como mais do que e:** nao impede um publish local (quem tem o token ainda manda um POST) e **alarga quem consegue publicar** — de uma pessoa para quem tiver write em `.github/workflows/`. Ganho de rastro **e** alargamento de acesso; se incomodar, um `NIDUM_PUBLISH_TOKEN` separado permite revogar o publish sem cegar o conferidor. Quem **pega** um publish fora do repositorio continua sendo o conferidor (D-anterior, classe `publicado_divergente`): os dois trabalham juntos, nenhum resolve sozinho. **A trava A entra junto, e sozinha so documenta** (PR #90): recusa PR que altere um publicado sem subir o `version:`. Nao teria impedido o caso de 11/09 — teria deixado rastro dele. |
| D64 | **Versão igual com conteúdo diferente: a simulação AVISA e NÃO trava — e o motivo é o caso concreto** (11/09/2026) — registro pedido pelo Davi, *"é a decisão mais fácil de reverter errado daqui a seis meses"* | **O instinto é travar, e o instinto está quase certo.** Painel e repo dizendo a mesma versão com o conteúdo diferente foi *exatamente* a forma que a divergência de 11/09 tomou para ficar invisível: o número afirmava que eram iguais, e quem lia acreditava nele em vez de conferir. Uma trava ali parece a resposta óbvia. **O CASO CONCRETO QUE A DESMENTE — e é por isto que este registro existe:** a primeira publicação do conserto **cai exatamente nesse caso**. O painel está em `chatnd` 1.65.0 e o repo em 1.65.0, com **1.148 linhas realmente diferentes** (gerador: 2.8.2 × 2.8.2, **543 linhas**). Uma trava em *"versão igual + conteúdo diferente"* **bloquearia justamente a rodada que vem consertar o problema que ela existe para prevenir.** **A regra geral que isso ensina:** uma trava desenhada a partir de um incidente precisa ser testada contra o *conserto* do incidente, e não só contra o incidente. Trava que impede a correção do caso que a motivou está invertida — protege o estado ruim. **ONDE A TRAVA VAI, ENTÃO:** no PR (a trava A, `trava_bump.py`), onde o conserto custa **uma linha** e ninguém está no meio de uma publicação. Travar no momento do publish é caro e tarde; travar no PR é barato e cedo. **QUANDO REVER:** depois que o publicado e o repo convergirem — passado o primeiro publish, *versão igual com conteúdo diferente* volta a ser um estado que não deveria existir, e aí travar no publish passa a ser barato. Quem reabrir isto: confira antes se a convergência aconteceu; o aviso no `_publicar_comum.py` continua imprimindo o estado. |
| D65 | **Os dois publicadores do `relatorio_ambientes_nidum` discordam do NOME da tool — registrado ANTES de arquivar o antigo** (11/09/2026) — ordem pedida pelo Davi | **O caso.** `publicar_relatorio_ambientes.py` é um duplicado antigo e **hardcoded** do `publicar_tool.py` (mesmos endpoints, id/nome/descrição fixos no script). Os dois discordam: o script manda `Relatorio de Ambientes (modelo visual)`; o cabeçalho `title:` do arquivo no repo diz `Relatorio de Ambientes Nidum`. **A consequência operacional, HOJE:** publicar esse alvo pelo caminho genérico (o workflow do Actions, que lê o cabeçalho — que está no repo, que é o ponto) **renomeia a tool no painel**. **POR QUE ISTO É REGISTRO E NÃO NOTA DE RODAPÉ** (palavras do Davi): *"cosmético e reversível" só é verdade para quem sabe que aconteceu.* Uma mudança pequena e silenciosa num nome que outras pessoas usam para achar a ferramenta não é cosmética para quem não foi avisado — é a ferramenta que sumiu. Reversível e *percebido* são propriedades diferentes, e só a segunda depende de estar escrito. **O que fica:** o cabeçalho do arquivo é a fonte da verdade (está no repositório, versionado, e é o que o publicador genérico lê). O script antigo vai para `_arquivo/` em PR separada, **depois** deste registro. |
| D66 | **"Está à frente" não é "contém tudo" — vale para toda sincronização entre duas fontes que ambas mudaram** (11/09/2026) — generalização do Davi, a partir do `trazer_publicado.py` | **O caso que a produziu.** Medido o publicado (373.947 bytes) contra o repo (366.012), a conclusão *"o publicado está à frente, então traga o publicado por cima"* é tentadora e **errada**. O repo também andou: +779 bytes desde o retrato de 05/09. Copiar o publicado por cima apagaria as linhas que só existem no repo — **o mesmo erro que estávamos consertando, cometido na direção oposta**, e por cima de um trabalho que ninguém lembraria de procurar. **A FORMA GERAL:** entre duas fontes que ambas mudaram, *quem está à frente* é uma pergunta sobre **volume**; *pode copiar por cima* é uma pergunta sobre **continência**. Uma não responde a outra. A única cópia segura é quando um lado é **superconjunto** do outro. **ONDE ISTO JÁ APARECEU NESTA CASA, e é por isso que vira princípio e não nota:** a esteira mede `adicionar/atualizar/remover` em vez de espelhar a pasta; a conferência separa **órfão** de **faltante** em vez de contar "diferenças"; o `_tamanho_da_diferenca` foi refeito porque um número único não dizia a direção (D54). São três instâncias do mesmo erro: **medir a diferença como escalar quando ela é vetorial.** **NA PRÁTICA:** toda sincronização entre duas fontes vivas relata **dois números** — *só em A* e *só em B* — e recusa a operação destrutiva quando o número da ponta que seria sobrescrita não é zero. Recusar com a lista na mão é mais útil que copiar e torcer. |
| D67 | **`WEBUI_URL` vazia: a mitigação existia, a configuração ruim ficou, e ninguém amarrou as duas — sem dono por uma semana** (11/09/2026) — registro pedido pelo Davi | **Os dois fatos, e a distância entre eles.** Em 03/09 foi medido no laço agêntico que, com `WEBUI_URL` vazia, o link sai **relativo**, o modelo o reescreve na resposta, perde a barra inicial, e o navegador resolve contra `/c/<chat_id>` — **404**. A resposta foi construir o **anexo nativo** (tool 2.8.2, 05/09), que torna o link desnecessário quando chega. E foi acrescentado um `log.warning` dizendo que a configuração estava vazia. **Em 11/09 esse warning ainda estava saindo em produção, verbatim.** **O QUE FALHOU NÃO FOI A DETECÇÃO — ela funcionou o tempo todo.** O aviso estava no log, correto, desde o primeiro dia. O que não existia era alguém encarregado de **ligar o aviso ao conserto**: a mitigação foi tratada como solução, o defeito de configuração continuou aberto, e os dois viraram assuntos separados que ninguém cruzou. **POR QUE ISTO É PIOR QUE UM BUG:** um bug tem sintoma e alguém reclama. Isto tinha **mitigação funcionando** — o pipe entregava por link e ninguém sentia falta — então a configuração ruim ficou invisível exatamente porque a rede de segurança segurava. **Mitigação que funciona bem o bastante remove o incentivo de consertar a causa, e o registro precisa dizer isso.** **O que fecha:** `WEBUI_URL` preenchida no painel (Admin → Configurações → Geral → *WebUI URL*), pelo Davi, em 11/09. É `ConfigVar('WEBUI_URL', 'webui.url', ...)` — configuração **persistente**, então variável de ambiente **não pega** (D62); tem de ser o painel. **A REGRA QUE FICA:** `log.warning` sobre configuração ausente precisa ter um dono e um prazo, ou vira paisagem. Aviso que ninguém lê é aviso que não existe, e a diferença entre os dois só aparece quando a mitigação falha. |
| D68 | **O caso mais puro da família: o diagnóstico impossível POR CONSTRUÇÃO** (11/09/2026) — formulação do Davi: *"enquanto o `log.debug` estiver ali, toda causa tem o mesmo rosto"* | **O caso.** `utils/middleware.py`, dois pontos: `except Exception as e: log.debug(e); break`. Produção roda em INFO — a linha **não existe**. A exceção some, o laço quebra, e a resposta sai com o que já tinha acumulado. Em 11/09: *"5 Origens"* e nenhum texto, sem erro na tela, sem traceback. **POR QUE ESTE É O CASO MAIS PURO, e a distinção é do Davi:** a família já tinha dois tipos conhecidos — a **guarda no lugar errado** (o freio de catástrofe antes do `if confirmado`; o modo seguro que desliga a proteção, D58) e a **comparação que nunca acontece** (a classe não conferida contada como limpa, D57; a barra final, D57). Este não é nenhum dos dois. **Aqui o mecanismo funciona exatamente como escrito** — captura, registra, quebra. Não há bug. O defeito é que a informação necessária para diagnosticar é **destruída pelo próprio desenho**, e nenhuma quantidade de investigação a recupera. **A CONSEQUÊNCIA QUE DÁ NOME AO CASO:** três causas possíveis — teto de iteração, estouro de contexto, quebra do provedor — entram por este `except` e **saem idênticas**. Não é que a investigação seja difícil: é que as hipóteses são **indistinguíveis por construção**. Foi exatamente o que aconteceu: o Davi levantou as três, e nenhuma podia ser descartada por evidência — só por raciocínio sobre código. **O TESTE PARA RECONHECER O PADRÃO:** *se esta falha acontecer, o que fica registrado permite dizer QUAL das causas foi?* Se a resposta é não, o problema não é o tratamento do erro — é que o sistema não é diagnosticável, e isso é um defeito de projeto, não de execução. **Conserto:** PR #95, `log.exception` com a contagem de iterações junto (é ela que separa *bateu no teto* de *quebrou antes*). **Nada passa a falhar que não falhava** — o `break` continua; a diferença é que existe registro. |
| D69 | **"Quem pôs isso em produção" era respondível o tempo todo — e virou arqueologia por ninguém ter perguntado ao sistema** (11/09/2026) | **O caso.** A divergência publicado × repo gerou uma investigação inteira: contar linhas, medir direções, procurar âncora em 500 commits, achar um retrato de 05/09 no disco para datar a mudança. Trabalho de arqueologia, feito com diffs. **E o painel guardava a resposta desde sempre:** `FunctionModel` e `ToolModel` têm **`user_id`, `created_at` e `updated_at`**. *Quem* e *quando* eram uma consulta, não uma dedução. **O QUE FALHOU FOI A PERGUNTA, NÃO O DADO.** Ninguém perguntou ao sistema porque ninguém supôs que ele soubesse — e a investigação por diff é *plausível o bastante* para não levantar suspeita de que havia caminho melhor. Método que funciona é o que impede de procurar o método que funcionaria melhor. **A REGRA QUE FICA:** antes de reconstruir um fato por inferência, conferir se a fonte **guarda** o fato. Em particular para *quem* e *quando* — que quase todo sistema registra, e quase toda investigação reconstrói. **Conserto:** o `_diff_publicado` passa a imprimir `ultima gravacao: <data> por <nome>`, resolvendo o `user_id` pelo `/api/v1/users/{id}`. |
| D70 | **O "Chico" está aposentado — a ressalva do item #1 da Fase E deixa de ser bloqueio, MAS a nota fica** (11/09/2026) — decisão do Davi | **O que muda.** O item #1 (classificador de rota) carregava uma ressalva séria: o *Chico* — motor de outro colaborador — usa o `chatnd` como **base model**, e mudar as categorias do roteador atravessava produto. *"O corte não é só nosso."* Com o Chico aposentado, o corte volta a ser só nosso e o #1 deixa de depender de terceiro. **POR QUE A NOTA FICA, e esta é a parte que importa para quem ler depois:** a dependência não foi **resolvida** — foi **suspensa**. O acoplamento técnico continua existindo no código (um preset usando o `chatnd` como base model herda as categorias do roteador); o que sumiu foi o consumidor. **Se o Chico voltar, a ressalva volta a valer integralmente, e volta a ser pré-requisito do #1 — não uma consideração.** **A distinção que este registro existe para preservar:** *bloqueio removido* e *bloqueio adormecido* se parecem enquanto duram e se comportam de forma oposta quando a condição muda. Registrar só "deixou de ser bloqueio" produziria, meses depois, alguém mexendo nas categorias com o Chico de volta no ar — e o registro teria sido a causa. |
| D71 | **Helper testado, fiação não — e é diferente de tudo o mais desta semana porque NÃO há nada errado com a função nem com o teste** (12/09/2026) — formulação do Davi | **O caso.** A conferência diária relatou **62 órfãos**; 56 eram as fichas da Fase D. Existe uma função inteira para impedir exatamente isso — `separar_derivados` — com docstring dizendo que sem ela as fichas *"virariam 56 órfãos, todo dia, para sempre"*, e que **órfão é a categoria cuja reconciliação é apagar**. **A função estava correta. O teste dela existia e passava. Ela era chamada, no lugar certo, com os argumentos certos.** E mesmo assim o defeito acontecia: o `return`, duas linhas abaixo, recalculava `comparar(esperado, base_chaves)` e **descartava** o resultado filtrado. O chamador recebia um valor que nunca passou pelo filtro. **POR QUE ESTA CLASSE É NOVA no registro desta casa.** As anteriores tinham sempre um culpado localizável: guarda no lugar errado (D58), comparação que nunca acontece (D57), diagnóstico destruído por construção (D68), nome que não existe (o `EA`). **Aqui não há linha errada.** Cada peça, isolada, está certa — e a soma não faz o que a soma promete. O buraco está **entre** as peças, e é invisível para qualquer teste que olhe uma peça. **O QUE CUSTOU:** a Fase D inteira aparecia como candidata a remoção em todo relatório diário desde que entrou na base. Nada foi apagado — a conferência só relata e o sync compara repo×base — mas o alarme apontava para o precipício que a própria função existia para evitar. **O MÉTODO QUE FICA, e vale mais que o conserto (uma linha):** *testar o valor que o chamador de verdade recebe, e não só o helper.* Na prática: trocar as fronteiras (rede, disco) por dublês e chamar **a função que o workflow chama**, olhando o que sai. `teste_fiacao_conferir.py` faz isso, e foi verificado do jeito que importa — **reintroduzi o bug de propósito e o teste ficou vermelho em 4 casos**. Teste que passa antes e depois do conserto não prova nada. |
| D72 | **REGRA: teste novo de conserto só vale depois de falhar com o defeito de volta** (12/09/2026) — formulação do Davi, a partir do método usado no D71 | **A regra, inteira:** ao consertar um defeito e escrever o teste que o impede de voltar, **reintroduza o defeito de propósito e confirme que o teste fica vermelho**. Só então o conserto está pronto. **POR QUE É REGRA E NÃO ZELO.** Um teste escrito *depois* do conserto é escrito olhando para o código já certo — e o jeito mais fácil de escrever um teste assim é descrever o que o código faz, que é exatamente o teste que **não** pega a regressão. Ele passa hoje, passa amanhã, e passaria também se o defeito voltasse por outra porta. **É a diferença entre teste que PROVA e teste que ACOMPANHA**, e ela não aparece em nenhuma métrica: cobertura, contagem de asserts e CI verde são idênticos nos dois casos. **O CASO QUE A PRODUZIU (D71).** `separar_derivados` tinha teste, o teste passava, e a função estava correta — e mesmo assim as 56 fichas apareciam como órfãs, porque o `return` descartava o resultado filtrado. O teste existente **acompanhava** a função; nenhum teste **provava** o valor que o chamador recebe. Ao escrever `teste_fiacao_conferir.py`, o defeito foi reintroduzido de propósito: **4 casos ficaram vermelhos**. Sem essa verificação não haveria como distinguir um teste novo que pega o defeito de um teste novo que apenas descreve o conserto. **NA PRÁTICA, e é barato:** antes de commitar, desfaça a linha do conserto, rode o teste, confirme o vermelho, refaça. Leva menos de um minuto e é a única evidência de que o teste tem relação causal com o defeito. **O que isto NÃO exige:** não vale para teste de comportamento novo (não há defeito para reintroduzir) nem para prova de propriedade geral. É especificamente para o teste que nasce junto de um conserto — que é onde a ilusão mora. **COROLÁRIO (21/09/2026, formulação do Davi): quando um teste novo falha, conserte a FIXTURE, nunca a asserção.** Ao escrever os 16 casos do corte do orçamento, dois ficaram vermelhos — o material de teste era curto demais e não forçava o corte, então a lista voltava inteira. Havia duas saídas: aumentar o material (fixture) ou afrouxar a asserção de `0 < len < 8` para `len <= 8`. **A segunda faria o teste passar sem provar nada** — e passaria igual com o defeito de volta, que é precisamente o que o D72 existe para impedir. **Afrouxar a verificação para o teste passar é o jeito mais comum de um teste mentir**, e é mais perigoso que o teste ausente: o ausente ninguém conta como prova. Regra prática: se a asserção estava certa quando você a escreveu, ela continua certa depois de falhar — o que mudou de lugar foi o cenário. |
| D73 | **REGRA: quando a casa já tem a definição, medir com outra é criar uma segunda verdade** (12/09/2026) — formulação do Davi, a partir da régua de ambição de deck | **O caso.** A régua do item #6 da Fase E ganhou uma trava: *zero slides mudos*. Eu a implementei contando elementos de texto por slide — **definição minha, inventada para a medição**. O baseline do pipe acusou **3 mudos** numa das execuções. Fui verificar antes de chamar de defeito, e o comentário do próprio gerador respondia: `# Tipos que ficam MUDOS sem corpo. 'capa', 'secao' e 'encerramento' sao legitimos sem corpo - nao entram aqui`, com a lista em `_TIPOS_EXIGEM_CORPO`. **Os 3 eram capa, seção e encerramento** — exatamente o que um deck bem formado tem. **O QUE TERIA ACONTECIDO sem a verificação:** a régua reprovaria o agente por produzir capa. E o número seria *apresentado como medição*, com a autoridade que número tem — reprovando um comportamento correto por uma definição que ninguém reviu. **POR QUE É CLASSE E NÃO DESCUIDO.** Definir a métrica na hora de medir é o caminho natural: quem mede está pensando no número, não no vocabulário do sistema. Mas a casa **já tinha** a definição, escrita, comentada e usada pelo código que valida de verdade. Duas definições de "mudo" produzem dois vereditos sobre o mesmo slide, e o veredito da medição é o que vira decisão. É o D66 ("está à frente" ≠ "contém tudo") aplicado a **critério** em vez de a **conteúdo**: duas fontes da mesma verdade, ambas plausíveis, e nada arbitrando. **NA PRÁTICA:** antes de escrever uma métrica, procurar se o código já nomeia aquilo. Se nomear, **importar a definição, não reescrevê-la** — e se a definição da casa parecer errada, o trabalho é mudá-la lá, não medir por fora. **O baseline que produziu o achado, e ele vale por si:** 5 execuções do mesmo pedido no pipe — 23, 23, 17, 21 slides (uma falhou sem gerar arquivo), **mediana 22, não os 18** de que partimos. A variação **17–23 em quatro execuções** é o argumento mais forte pela régua de mediana-de-cinco: com uma execução de cada lado, a diferença medida pode ser metade ou o dobro da real. |
| D74 | **O cache da tool no pipe NÃO se auto-invalida — e o "funcionou" de 11/09 ensinou errado** (12/09/2026) | **A correção, e ela é minha.** Em 11/09 afirmei que a regra do `CLAUDE.md` (*"ao atualizar só a tool, republicar também o pipe — reseta o cache"*) estava desatualizada. Baseei-me no `get_tool_module_from_cache`, que compara o conteúdo do banco e recarrega sozinho. **Mas esse é o caminho do middleware.** O pipe usa outro: `_get_tool` guarda em `self._tool_cache` (chatnd.py:5614) e **não invalida nunca** — só zera quando o processo reinicia ou o pipe é republicado. **A regra estava certa.** **POR QUE EU ACREDITEI NO CONTRÁRIO:** publiquei o gerador e o teste mostrou o código novo rodando. Conclusão natural: o cache se resolveu. **A causa era outra** — o deploy do PR #95, que o Davi tinha acabado de mergear, reiniciou o contêiner minutos antes. O cache não se invalidou; ele foi **destruído por um evento sem relação**. **É O TIPO DE "FUNCIONOU" QUE ENSINA ERRADO** (palavras do Davi), e a forma é geral: um teste que passa por causa de uma condição que ninguém registrou produz uma conclusão **mais** confiante que o normal — porque tem evidência empírica do lado errado. Não foi ausência de prova; foi prova de outra coisa. **O sintoma que isso produziria em operação:** publicar só a tool, testar logo em seguida **sem** reiniciar, e ver o comportamento ANTIGO — concluindo que o publish não funcionou, ou pior, que o código novo está errado. **O que fica:** a regra do `CLAUDE.md` permanece como está. O `15_Operar_Sozinho` passa a dizer, na seção de publicar: **tool sozinha exige republicar o pipe, ou reiniciar.** |
| D75 | **Rename dos repositórios: `nidum-platform` → `chatnd` e `nidum-chatnd-basefonte` → `chatnd-conhecimento`** (12/09/2026) — com o levantamento prévio e a prova de cada risco | **Por que renomear:** `nidum-platform` não dizia o que tinha dentro e `nidum-chatnd-basefonte` confundia. **Nada quebrou** — mas o valor deste registro não é esse: é a lista e o método, para o próximo rename não precisar ser descoberto de novo. **OS TRÊS RISCOS E COMO CADA UM FOI PROVADO:** **(1) Conexão Railway ↔ GitHub** — era o maior, e o único impossível de verificar antes. Se não seguisse o rename, o sintoma seria *deploy que para de disparar no push*, em silêncio. **Prova: um push trivial.** Disparou deploy em 4 minutos. **(2) `ESTEIRA_TOKEN` (PAT fine-grained)** — a seleção de repositório de um PAT liga por **ID**, não por nome, então a expectativa era sobreviver. O sintoma de falha seria enganoso: a conferência **não quebra**, ela relata *"duas classes ficam de fora"*, que se lê como configuração ausente e não como token quebrado. **Prova: rodar o conferidor e exigir que NENHUMA classe saia como não conferida.** As seis rodaram. **(3) Clone cruzado com o nome cravado** — `conferir_registros.yml` clona a esteira por URL literal. **Funcionou pelo redirecionamento do GitHub**, que cobre clone/fetch/push/API indefinidamente — até alguém criar um repositório novo com o nome antigo. **Prova: o clone no mesmo run do conferidor.** Corrigido para o nome novo depois, porque depender de redirecionamento é depender de um nome que ninguém mais controla. **O QUE O LEVANTAMENTO ACHOU E NÃO ERA DO RENAME** — e este é o subproduto que vale: cinco docstrings mandavam `cd C:\Users\daviv\dev\nidum-platform`, e **`dev\` não existe nesta máquina**; a doc 04 dava o projeto Railway como `surprising-flow` e o serviço como `nidum-platform`, quando **ambos se chamam `ChatND`** (conferido por `railway status`). Varredura feita por um motivo encontra defeitos de outro. **O QUE NÃO FOI TOCADO, DE PROPÓSITO:** `07_Diario` e `08_Decisoes` mantêm os nomes antigos. **São registro, não instrução** — uma entrada de julho que diz `nidum-platform` era verdade em julho, e reescrevê-la para casar com um rename de setembro **falsifica o registro**. O rename entra como fato novo, não como correção do passado. **O que NÃO é renomeado junto:** o volume do Railway (`nidum-platform-volume-YLGF`) e os ids de produção. |
| D76 | **O rename do repositório NÃO toca nenhum id de produção — verificado, não suposto** (12/09/2026) — pergunta do Davi | **A pergunta:** *"confirma que o `chatnd` como id da function e do modelo não mudou com o rename? Se algum id tivesse mudado, presets e valves quebrariam."* **A resposta é não, e o motivo é estrutural:** id de function, de tool e de modelo vivem no **banco do Open WebUI**, gravados no publish. O nome do repositório no GitHub não participa de nenhum deles — os dois mundos só se tocam no momento em que alguém publica, e o que viaja é o **conteúdo** do arquivo, nunca o caminho. **Conferido em produção depois do rename, e não deduzido:** `GET /api/v1/functions/id/chatnd` → `id='chatnd'`, `name='ChatND'`, `is_active=True`, versão publicada 1.66.0. `GET /api/models` → `chatnd` presente, e os quatro dependentes vivos: `chico-m1`, `nidum-identificador-ambientes`, `nidum-10---documentos`, `nidum-10---dia-a-dia`. `GET /api/v1/tools/export` → os seis ids intactos. **POR QUE VALIDAR O ÓBVIO AINDA ASSIM:** a pergunta separa duas coisas que parecem uma — *o repositório* e *o que está no ar*. Esta casa já pagou caro por confundi-las (o publicado que não correspondia a commit nenhum). Um "claro que não muda" sem consulta seria exatamente o tipo de certeza que o D74 registra como perigosa. |
| D77 | **REGRA: varrer por string é barato; decidir o que a string SIGNIFICA em cada ocorrência é o trabalho** (12/09/2026) — formulação do Davi, a partir do rename dos repositórios | **O caso.** O rename exigiu trocar `nidum-platform` e `nidum-chatnd-basefonte` por `chatnd` e `chatnd-conhecimento`. Um *find-and-replace* nas 55 ocorrências levaria trinta segundos e teria **corrompido duas coisas certas**: **(1) `nidum-platform-volume-YLGF`** — nome do **volume** no Railway, não do repositório. Volume **não** é renomeado junto; trocar na fixture do `teste_vigilancia.py` a faria mentir sobre o que a API devolve. **(2) `nidumtec26/nidum-platform`** — **outro repositório**, de outro dono, arquivado, citado no `MAPA_REAL.md` da esteira. Nós não o renomeamos; a referência estava correta e continuaria correta. **E uma terceira decisão, que é de natureza diferente:** `07_Diario` e `08_Decisoes` mantêm os nomes antigos **de propósito**. São **registro, não instrução** — uma entrada de julho que diz `nidum-platform` era verdade em julho. Reescrevê-la para casar com um rename de setembro **falsifica o registro**: passa a alegar que em julho o repositório se chamava outra coisa. O rename entra como **fato novo** (D75), não como correção do passado. **A FORMA GERAL.** Toda varredura por texto devolve três baldes, e só o primeiro é mecânico: *o mesmo nome pela mesma razão* (troca), *o mesmo nome por outra razão* (não toca — volume, repo de terceiro), e *o nome certo no tempo errado* (não toca — registro datado). A ferramenta enxerga um balde só. **NA PRÁTICA:** nenhuma substituição em massa sem ler **cada** ocorrência e dizer a qual balde ela pertence. Se a lista for longa demais para isso, ela é longa demais para ser substituída em massa. **O subproduto que confirma a regra:** a mesma varredura, feita à mão, achou dois defeitos **que não eram do rename** — cinco docstrings mandando `cd` para uma pasta que não existe nesta máquina, e a doc 04 dando nomes errados para o projeto e o serviço do Railway. Varredura feita por um motivo encontra defeito de outro, **desde que alguém leia**. |
| D78 | **O `CLAUDE.md` da raiz ganha cópia versionada — no repositório PRIVADO, e não no público** (12/09/2026) | **O problema:** o `CLAUDE.md` que o Claude Code carrega em toda sessão vive na raiz da área de trabalho, **fora dos dois repositórios**, e portanto **sumiria junto com a máquina**. **Por que ele está fora, e isso não é descuido:** ele é carregado porque é a **pasta-mãe** das duas frentes — estando ali, vale para a plataforma e para a esteira ao mesmo tempo. Movê-lo para dentro de um repositório o faria **parar de ser lido** quando o trabalho fosse no outro. A posição é funcional, não acidental. **A DECISÃO INVERTEU A INCLINAÇÃO INICIAL, e o motivo é verificável:** a ideia era versioná-lo no `chatnd`. **O `chatnd` é PÚBLICO** (conferido por `gh repo view`). O documento nomeia colaboradores e seus papéis, aponta a casa das obras do fundador e mapeia três projetos vizinhos. Nada é segredo — e nada precisa estar na internet aberta. O `chatnd-conhecimento` é **privado** e já guarda o acervo, material da mesma natureza. A cópia foi para `chatnd-conhecimento/_docs/CLAUDE_raiz.md`. **O QUE MUDA NO FLUXO: nada.** A cópia da raiz continua sendo a carregada; a versionada é backup e fonte de restauração. **Duas cópias são duas verdades (D73)** — e aqui a comparação **não pode ser automática**, porque nenhum CI enxerga um arquivo fora do repositório. Mitigação proposta e não implementada: uma classe `raiz_divergente` no conferidor de registros, que compara quando o arquivo está ao alcance e relata **não conferida** quando não está (mesmo padrão do D57). **Achado lateral que o Davi deve saber:** o `chatnd` sendo público, o `CLAUDE.md` **da plataforma** já está na internet — e ele declara os provedores e modelos (Claude Sonnet, gpt-5-mini, gpt-5.1). Não contradiz a regra do modelo invisível, cujo escopo é o **texto das respostas** (D61), mas é informação pública que talvez ninguém tenha decidido publicar. |
| D79 | **Áudio anexado (#8) e saída de voz (#9) saem da Fase E — descarte com motivo** (12/09/2026) — decisão do Davi | **A decisão:** *não são necessários.* Os dois deixam de ser itens do corte. **O que eram, e por que a lista os trazia:** o #8 é entrada por voz (transcrição por Whisper local, que já está no volume) e o #9 é a saída falada (player, keepalive SSE, cancelamento limpo, retenção de 30 dias). O dimensionamento de 10/09 os classificou como **alto valor** — o #8 como *"barato de manter, caro de perder"* e o #9 como *"o item que mais aparece se sumir"*. **POR QUE O DESCARTE NÃO CONTRADIZ AQUELA LEITURA.** Ela media o custo de **perder** um recurso existente, não a necessidade dele. São perguntas diferentes, e só a segunda decide o escopo de um corte: *portar isto é pré-requisito para o agente substituir o pipe?* Para voz, não é — o produto funciona sem, e ninguém pediu. **O QUE ISTO CUSTA, dito para não ser descoberto depois:** o corte passa a significar **perder entrada e saída de voz** enquanto o agente não as tiver. Não é adiamento silencioso — é a consequência aceita. Se alguém sentir falta depois do corte, a resposta certa é reabrir com o caso de uso na mão, **não** tratar como regressão: ninguém prometeu que sobreviveriam. **Efeito na lista:** restam **dois** itens para o corte — o **#13** (orçamento por pasta, que absorveu o #3) e o **#6** (ambição de deck). Com os dois fechados, o corte vira decisão de data. |
| D80 | **O AGENTE PODE PUXAR 26× O QUE O PIPE PUXA NUM TURNO — e um documento no p90 estoura o teto do pipe sozinho** (12/09/2026) — medido, não estimado; registro com destaque pedido pelo Davi | **Os números, medidos em produção:** chunk médio **1.718 chars** (`CHUNK_SIZE=2200`, overlap 300, sobre 34.245 chunks) · `query_knowledge_files(count=5)` = **8.590 chars por chamada** · `view_file` padrão **10.000**, **hard cap 100.000** · teto do laço agêntico **12 rodadas** · acervo: 888 `.md`, mediana 6.856, **p90 48.156**, maior **1.165.663**. **A conta por turno:** só com busca, ~103.000. Com `view_file` no padrão, 120.000. Com `view_file` no hard cap, **até 1.200.000**. O orçamento do pipe para o canal da pasta é **45.000** (`MAX_CHARS_PROJETO`). **POR QUE ISTO MUDA A LEITURA DE OUTRAS COISAS, e é a razão do destaque:** durante a semana, lentidão e resposta truncada no agente foram atribuídas a teto de iteração, a estouro de contexto do provedor e a falha do laço. **Todas eram explicações plausíveis para um sintoma que este número também explica** — e que nenhuma delas mediu. Um turno que puxa 1,2 milhão de caracteres é lento e é truncado pelo provedor, e a causa não está no laço: está em quanto ele pôde pedir. **A DIFERENÇA DE FORMA, e não de grau:** o pipe **monta uma vez** — recupera, monta o bloco, corta, entrega. O agente **acumula chamada a chamada**. Por isso teto **por chamada** não limita nada (o modelo chama de novo), e o orçamento tem de ser **por turno** e cobrir **todas** as tools de conteúdo — com `ENABLE_KB_EXEC=False` o agente recebe seis, e um teto só na busca é contornado pelo `view_file` sem ninguém perceber. **Para quem investigar lentidão ou resposta truncada daqui em diante:** meça o material servido no turno **antes** de teorizar sobre o laço. O número existe agora (modo seco do orçamento) e é a primeira coisa a olhar. |
| D81 | **DÍVIDA: o agente não passa pelo analytics do pipe — e isso vai doer no dia do corte** (12/09/2026) — anotada pelo Davi, deliberadamente não resolvida agora | **O que é:** o pipe grava cada turno na tabela `eventos` (rota, travas, chars por canal, tokens, latência — content-free). **O agente não grava nada**: ele é preset com function calling nativo, e o caminho dele no middleware não conhece aquela tabela. **Por que a dívida é aceitável hoje:** a medição do orçamento por turno (#13) precisa de uma janela definida, não de observabilidade permanente — o número sai em linha de log e é colhido por `railway logs` durante a medição. Para isso, log basta. **POR QUE ELA VAI DOER, e é o ponto de registrar agora:** no dia do corte a pergunta central é **comparar os dois** — o pipe e o agente, mesma pergunta, mesmo acervo. O pipe tem série histórica; o agente não tem nada. A comparação seria entre um lado medido e outro lembrado, e **isso não é comparação**. A linha de base de 20 origens (D59) mediu o pipe; não há equivalente do outro lado. **O agravante que o D74 já nomeou:** log rotaciona. Uma conclusão tirada de log que depois some é uma conclusão que ninguém pode reconferir — e este projeto já registrou o que acontece quando se conclui a partir de evidência que desaparece. **O que fecharia:** eventos do agente na mesma tabela, com as mesmas colunas content-free — ~meio dia, backend. **Não fazer agora é decisão, não esquecimento**; o custo de não ter aparece no dia da comparação, e é aí que ele é caro demais para pagar. |
| D82 | **A base de conhecimento passa a 4 bases — Projetos · Reuniões · Fonte · Ecossistemas — na ordem de prioridade das perguntas** (16/09/2026) — decisão do Davi | **A decisão:** *"grande parte das perguntas se concentrarão sobre os projetos"*; a ordem principal é **1 Projetos, 2 Reuniões, 3 Fonte, 4 demais Ecossistemas**. As 11 bases de 05/09 (uma por pasta-mãe) viram **4**: `Projetos` (`accc1ac9…`, nova, recebe a pasta-mãe nova `0 - Projetos`), `Reuniões` e `Fonte` (as mesmas de 05/09) e `Ecossistemas` (`7393449f…`, nova, recebe as 11 pastas-mãe dos acervos, inclusive a nova `EGP`). **Regra de conteúdo, reafirmada na mesma decisão — NÃO entra na base:** formato não suportado, arquivo **pessoal**, arquivo de **coautor** da seção jurídica, dado sensível. De resto, tudo entra. **Onde vive:** `chatnd-conhecimento` PR *eixo: 4 bases* (`sync_config.json` com `pastas_mae`, escopo com `0 - Projetos`/`EGP`, `coautores intensivo` excluído em duas camadas; runbook `_docs/EIXO_4_BASES.md`). O merge daquele PR é o corte; a Rodada A é um `resync` add-only (zero remoção, zero freio). **O achado que a decisão evitou por um dia:** a carga de 16/09 trouxe **61 `.md`** de `Juridico/Coautores Intensivo/<Pessoa>/` (Termo de Confidencialidade + Identificação de ~37 pessoas) para o repo da esteira; **não chegaram à base por acidente** — o freio em Operações (vermelho desde 15/09) abortava a rodada antes de Jurídico, e a conferência diária os listava como *faltantes, a próxima carga traz*. Removidos do repo em commit separado; **purga do histórico do git é decisão pendente**. **O que a decisão pede e o pipe ainda NÃO faz:** a *ordem* entre as 4. Com `MAPA_COLECOES` o pipe busca cada base com k igual e intercala em ordem de **id** (`sorted`), e o `DIAL_FASE3` só distingue Fonte × resto. Expressar a prioridade exige fatia própria no pipe (ordem do JSON e/ou k por base), **com medição antes** — acordo de fases. No preset agêntico a ordem cabe no *system prompt*. **Painel (Davi), sem o que a mudança não chega ao chat:** `MAPA_COLECOES` com as 4; `MAPA_K_POR_COLECAO` 8→12 (medir: 4×8 = 32 trechos contra os 88 de hoje); `BASE_CONHECIMENTO_ID` com as 4; as **9 bases antigas ficam** até a conferência bater — o `ChatND Agente beta` lista **todas** as bases que a chave enxerga, então enquanto existirem o agente vê 13. |
| D83 | **O agrupamento em `Ecossistemas` NÃO vale — ficam as 11 bases, entra só `Projetos`** (17/09/2026) — decisão do Davi, revendo a D82 no mesmo dia | **A decisão:** *"não precisa juntar todos os demais ecossistemas em uma única base — são muitos arquivos e a chance de delírio ou resposta errada aumenta; pode manter as bases como estava, apenas adicione a base PROJETOS."* Config em vigor: **12 bases** = as 11 de 05/09 + `Projetos` (`accc1ac9…`); `EGP` entra em Gestão de Projetos via `pastas_mae`. **Por que a reversão foi barata:** as 11 ficaram fora do config de 4 bases e a esteira, por desenho, não toca base fora do config — estavam intactas. **O que a D82 deixou para trás:** o sync disparado pelo merge escreveu **323 arquivos em `Ecossistemas`** e parou com 502: **o ChatND reiniciou ~01:53 UTC sob a carga de indexação** (323 arquivos em ~11 min). A base é duplicata — apagar no painel. O reinício sob carga é achado próprio: a Rodada A de 05/09 (379 arquivos) passou; esta não. Sem causa apurada ainda. **Painel, versão revista:** `MAPA_COLECOES` = as 11 + `"projetos"` (12 ids); `MAPA_K_POR_COLECAO` fica em 8; apagar `Ecossistemas`. A pendência da *ordem* Projetos > Reuniões > Fonte > resto continua (D82). |
| D84 | **`EGP` e `0 - Projetos` guardam CÓPIAS dos mesmos documentos — e a duplicata fica, com o custo declarado** (17/09/2026) — decisão do Davi, com o número na mão | **O que a medição achou**, ao investigar por que `Gestão de Projetos` pedia 21 remoções de 23: a reorganização do SharePoint **não moveu** os documentos do escritório de projetos para `0 - Projetos` — **copiou**. Os dois caminhos existem ao mesmo tempo. **14 nomes coincidem**: 7 com corpo idêntico e **7 com corpo divergente — todos os `FAZ_Cronograma_*` da Fazenda Fortaleza**, com a versão de `EGP` consistentemente ~2.500 chars maior (ex.: `26_07_21_v1` = 19.223 chars em Projetos contra 21.168 em EGP). **8 documentos existem só em `EGP`** (atas do Comitê de Projetos 14/08, 21/08 e 24/08; Governança EGP; Processo GP; Metodologia de Gestão de Projetos; Descrição de Ativos Serra Negra). **A decisão: manter (opção b).** `EGP` continua alimentando a base `Gestão de Projetos` e `0 - Projetos` a base `Projetos`. **O CUSTO, dito agora para não ser descoberto numa resposta errada:** uma pergunta sobre o cronograma da Fazenda Fortaleza recupera **duas versões do mesmo cronograma, de bases diferentes, que discordam entre si** — e o modelo não tem como saber qual é a vigente. É a duplicata que o D83 evitou no agrupamento reaparecendo por outra porta: lá era volume, aqui é contradição. As duas alternativas recusadas: tirar `EGP` da base (perderia os 8 únicos) e `NAO_INDEXAR.txt` em `EGP/3.1.3 Portfólio de Projetos/1. Projeto Fazenda Fortaleza/` (mataria as 14 cópias e preservaria os 8). **A segunda continua disponível a qualquer momento, sem PR** — é marcador de pasta, decisão de conteúdo. **O que isto NÃO é:** não é defeito da esteira. Ela espelha o SharePoint, e o SharePoint tem as duas cópias. A correção durável é na origem. |
| D85 | **CLASSE: um turno que termina SEM RESPOSTA é pior que qualquer resposta incompleta que declare a lacuna** (21/09/2026) — formulação do Davi, a partir do modo de falha medido no `c1` | **O que aconteceu, medido:** com o orçamento por turno ligado e a paginação funcionando, o turno `c1` (*"leia o NIDUM_DOCUMENTO_CONSTITUICAO por inteiro e me faça um resumo executivo de cada seção"*) chamou `view_knowledge_file` **10 vezes**, pedindo offset atrás de offset, subiu o próprio teto de 50.000 para 300.000 ao planejar no meio, demandou **340.331 chars**, recebeu **140.254**, leu até **132.204 dos 258.361** do documento — e devolveu **235 caracteres**: *"O orçamento de leitura por turno foi atingido... Vou criar as tarefas de leitura e buscar os trechos restantes em blocos menores para cobrir o documento inteiro antes de escrever o resumo."* **Ele anunciou que ia continuar, e o turno acabou.** Para comparar: as respostas dos outros cinco turnos tiveram 775, 469, 4.098, 3.302 e 22.210 chars. **POR QUE É CLASSE E NÃO BUG DO CORTE:** a paginação funcionou exatamente como desenhada. O defeito é que **"ler tudo em partes" consome o turno inteiro** — cada continuação é uma rodada do laço, e o laço tem teto. Qualquer mecanismo que transforme uma leitura grande em muitas leituras pequenas tem esta falha, com ou sem orçamento. **O QUE A CLASSE EXIGE:** um turno precisa **reservar rodada para responder**. Resposta incompleta que diz *"li até a seção 7 de 14, segue o resumo do que li"* é útil; promessa de continuar não é. **O conserto provável, se um dia voltarmos:** garantir que sempre sobra rodada — não subir o teto. Subir o teto só adia o ponto em que o turno acaba lendo. |
| D86 | **A régua da mediana NUNCA mediu o corte — media a variância do agente** (21/09/2026) — erro de desenho reconhecido pelo Davi, que a aprovou | **A régua tinha dois números:** a cauda acima do teto some (objetivo) **e a mediana não se move** (prova de que o teto pegou só a cauda). **O erro, óbvio depois de escrito:** o corte **não toca turnos abaixo do teto**. Por construção, a mediana de um conjunto em que a maioria fica abaixo do teto **não pode** se mover por causa do corte — então o segundo número nunca teve poder de detectar nada. Ele só podia medir **outra coisa**. **E mediu:** entre as duas fases a mediana subiu **+197%** (16.817 → 50.000), e um teto não aumenta consumo. A causa era a variância do agente: o mesmo prompt `a1`, na mesma pasta, consumiu **532 chars numa rodada e 30.237 na outra — 57×**. **O QUE FICA, e vale para qualquer régua futura:** antes de adotar um critério, pergunte **por qual mecanismo ele poderia falhar**. Critério que não tem como dar errado não é critério conservador — é critério vazio, e ele **custa**: gastou chamadas, produziu um veredito (*"a mediana caiu, o teto virou mordaça"*) e teria sustentado uma decisão. **O agravante:** o medidor imprimia "a MEDIANA CAIU" para qualquer variação, inclusive subida — então o critério vazio vinha com diagnóstico errado embutido. **NÃO REUSAR ESTE TESTE.** Para medir efeito de teto, olhe os turnos **acima** dele: quantos, quanto foi cortado, e o que a resposta virou. |
| D87 | **O agente vai a produção SEM TETO — o orçamento por turno fica estacionado, desligado** (21/09/2026) — decisão do Davi | **A decisão:** o orçamento **sai do caminho crítico do corte do pipe**. `AGENTE_ORCAMENTO_ATIVO` foi **apagada** do Railway; o código fica como está, íntegro e desligado por default. Só voltamos a ele **se o consumo virar problema no uso real** — não por precaução. **O QUE SABEMOS, para quem retomar:** (1) o mecanismo funciona — corte estrutural preserva JSON válido e descarta os trechos **menos relevantes** (a busca já vem ordenada por similaridade); a paginação recalcula `next_offset` e o modelo **de fato pede a continuação**; o teto proporcional sobe dentro do turno quando o agente planeja. Tudo medido em produção em 21/09. (2) **O conserto provável NÃO é subir o teto** — é garantir que sempre sobra rodada para responder (ver D85). Subir o teto adia o ponto em que o turno acaba lendo sem responder. (3) A medição precisa de **repetição dos mesmos turnos**, porque a variância de 57× (D86) torna uma passada inconclusiva. **Onde está:** `backend/open_webui/utils/nidum_orcamento.py` (código e o porquê de cada número), `_nidum_manutencao/bateria_orcamento.py` (a bateria pela API), `medir_orcamento.py` (a régua), `teste_orcamento_turno.py` (32 casos, verde). **Custo de não ter:** um turno do agente pode puxar até 26× o que o pipe puxa (D80). A decisão aceita isso até haver sintoma. |
| D88 | **CLASSE: três portas separam a API do laço agêntico, e nenhuma falha com erro** (21/09/2026) — as três medidas no mesmo dia, ao montar a bateria do orçamento e a do deck | **AS TRÊS, na ordem em que aparecem:** (1) **`stream: false` não executa ferramenta.** `non_streaming_chat_response_handler` (middleware.py:3482-3628) tem **0** ocorrências de "tool"; `streaming_chat_response_handler` (3629-5330) tem **128**. O laço inteiro vive no caminho de streaming. (2) **Sem `chat_id` E `id` não há `event_emitter`** (`get_event_emitter_and_caller`:3007), e o laço de ferramenta vive **dentro** de `if event_emitter:` (3661). (3) **`tool_ids` tem de ir no CORPO.** O `toolIds` do preset é aplicado pela INTERFACE (`Chat.svelte:347` lê, `:2449` envia); o backend só lê `form_data.pop('tool_ids')` (2671). Anexar ao preset **não basta** para quem chama pela API. **O QUE FAZ DISSO UMA CLASSE, e não três bugs:** nenhuma devolve erro. A (1) devolve resposta com o modelo dizendo *"vou consultar as bases"* e parando; a (2) repassa o `tool_call` cru ao cliente com `finish_reason: tool_calls`; a (3) faz o agente responder, educadamente, *"não tenho acesso a nenhuma ferramenta chamada X"* e **listar as builtin** — o que parece falha de publicação ou de permissão, e me fez conferir o `access_control` das 7 tools antes de achar a causa. **Resposta bem-formada, log limpo, fenômeno ausente.** **ESCOPO, e ele importa para ninguém ler isto como incidente:** as três afetam **APENAS teste automatizado pela API**. Quem usa pela interface sempre passou pelo laço normalmente — `Chat.svelte` manda os quatro campos (`chat_id`, `session_id`, `id`, `stream`) em toda requisição, e aplica o `toolIds` do preset. **Nunca houve buraco em produção.** **NA PRÁTICA, para o próximo teste do agente pela API:** mande `stream: true`, `chat_id`, `id` e `tool_ids` — os quatro, sempre. E **conte as tool calls do turno**: zero chamadas com resposta bonita é o sintoma das três. |
| D89 | **REGRA: comparação só vale com o mesmo pedido, nos dois lados, no mesmo dia** (21/09/2026) — erro cometido pelos dois, Davi e Claude | **O QUE ACONTECEU:** a régua do deck comparava o agente contra um baseline de **22 slides** do pipe. Rodadas as 5 do pipe **com o mesmo pedido**, no mesmo dia, o pipe deu mediana **41**. O agente, 32. O veredito reportado foi *"mediana 32, **145% do pipe**, aprovado"*; o número real é **78%** — abaixo do alvo de 80%. **A inversão não veio de medir errado: veio de comparar com um número que media outra coisa.** **E o outro lado do mesmo erro:** a densidade do pipe tinha uma medição de **380 chars/slide**, feita uma vez, com outro pedido, fora do repositório. Contra os 281 do agente daria 74% — no limite dos ±25%, sugerindo problema. Medidas as 5 com o mesmo pedido, o pipe deu **277**: o agente está em **101%**. **A suspeita que o número velho criou não existia.** **POR QUE OS DOIS ERROS SÃO O MESMO:** um baseline antigo não é um baseline — é uma lembrança com aparência de medida. Ele não vem com o pedido que o gerou, não vem com a data, e não avisa quando o sistema mudou por baixo. Os dois números (22 e 380) eram verdadeiros quando foram tirados; nenhum era verdadeiro para a pergunta que estavam respondendo. **NA PRÁTICA:** antes de comparar dois caminhos, meça **os dois**, com o **mesmo pedido**, na **mesma janela**. Se um lado só tem número velho, ele não é baseline: é motivo para rodar de novo. Reusar sai mais caro que remedir — aqui custou um veredito invertido e uma suspeita falsa, na mesma medição. **Ver também [D86](#)**: lá o critério não podia detectar o que dizia detectar; aqui o critério estava certo e o ponto de comparação é que não era. |
| D90 | **REGRA DE MONITORAMENTO: confere-se o ARTEFATO, nunca a frase que o descreve** (21/09/2026) — formulação do Davi, a partir do achado mais importante para o dia do corte | **O CASO:** ao renderizar o deck do agente, a prosa dele anunciou *"o PowerPoint foi gerado com todos os **30 slides**, exatamente como especificado — sem alterações, cortes ou resumos"*. **O arquivo tem 32.** A estrutura pedida tinha 32, e o arquivo está certo — o que estava errado era o **relato do agente sobre a própria ação**, feito logo depois de executá-la. **POR QUE ISSO É O ACHADO MAIS IMPORTANTE PARA O CORTE:** parte do plano de monitoramento do primeiro dia depende do que o agente diz — se buscou, o que leu, quantos slides gerou, se truncou. **Esse canal acabou de se mostrar não confiável**, e de um jeito que passa despercebido: a frase era específica, segura, e tinha o formato de uma verificação (*"sem alterações, cortes ou resumos"*). Uma frase vaga levanta suspeita; uma frase precisa e errada, não. **É parente do [D85](#):** lá o agente prometeu continuar e o turno acabou; aqui ele afirmou ter feito e o número não bate. Nos dois, a diferença está entre **o que ele diz que fez** e **o que aconteceu**. **NA PRÁTICA, no primeiro dia:** todo sinal que importa se lê no artefato ou no log — contagem de slides no `.pptx`, tool calls no transcrito, chars no `nidum_orcamento`. **A prosa do agente entra como hipótese a conferir, nunca como evidência.** E isto vale também para o relato de erro: *"não encontrei nada no acervo"* precisa ser conferido contra as buscas que ele de fato fez. |
| D91 | **O deck está resolvido: agente e pipe produzem conteúdo equivalente** (21/09/2026) — leitura e decisão do Davi | **O MÉTODO, e ele é a parte reusável:** dois `.pptx` do **mesmo pedido**, gerados pelo **mesmo gerador**, cada um **perto da mediana do seu lado** — não o melhor de um contra o pior do outro. Pipe: execução 5, **41 slides** (mediana exata de [30, 38, 41, 42, 43]). Agente: execução 1, **32 slides** (mediana de [29, 29, 32, 33, 40]). **O do agente não foi um deck novo:** é a estrutura que a régua mediu, apenas renderizada pelo `gerador_de_arquivos_nidum` — com conferência de que ela chegou intacta (32 na estrutura, 32 no arquivo). Pedir um deck novo entregaria um arquivo que não é o que foi medido. **Lidos pelo Davi: conteúdo equivalente.** **OS NÚMEROS, e por que não decidiram:** slides 32 × 41 = **78%** (alvo ≥80%); densidade 281 × 277 = **101%** (alvo ±25%); conteúdo total **79%**; **zero slides mudos**. O 78% está a 2 pontos do limite, com dispersão alta dos dois lados (pipe 30–43, agente 29–40). **Rodar mais 10 execuções foi descartado** (Davi): com essa variância os números dizem apenas que os dois estão no mesmo patamar, e é isso que se precisava deles. **O que decide é a qualidade do conteúdo, e ela se julga lendo.** |

### INCIDENTE DE PRIVACIDADE — certidões de pessoas físicas indexadas (LGPD)

> **Registro cronológico**, mantido nesta forma porque a sequência de datas pode ser
> pedida. Tratamento interno conduzido pelo Davi; esta seção documenta o técnico.

| Quando | O quê |
|---|---|
| **03/09/2026** | **Entrada.** A reformulação das pastas do SharePoint (4 → 15 pastas-mãe) trouxe `Operações/` para o escopo. A carga automática do `puxar_sharepoint` (commit `171cee4`) ingeriu a árvore `Operações/2. Retrofit/RTF-001-Ipanema_Apto_Rio de Janeiro/03-DUE DILIGENCE SIMPLIFICADA/Certidões/` — **25 arquivos**. |
| 03/09/2026 | Os arquivos passaram a ser classificados para `nd-normas`, a coleção que **todo o ChatND consulta**. |
| **04/09/2026** | **Detecção.** Durante verificação das pastas soberanas pedida pelo Davi, a varredura do acervo encontrou a árvore. Medido: **24 dos 25 arquivos contêm CPF com dígito verificador válido**. |
| 04/09/2026 | **Contenção (regra).** PR #145 na esteira: exclusão da árvore inteira de due diligence, por segmento de pasta. |
| 04/09/2026 | **Quarto caso.** O detector de entrada (PR #146) encontrou `DOCUMENTOS PARA DUO DILIGENCE` — grafia com erro — em **quatro** operações, mais **22 arquivos** que a primeira versão da regra não pegava. Regra corrigida no mesmo PR #145. |
| 04/09/2026 | **Remoção do índice: EXECUTADA.** 37 arquivos removidos (35 `nd-normas`, 2 `nd-projetos`), **zero falhas**, com dry-run antes conforme D27. A conferência de órfãos do mesmo dia deu **zero** — ver D28. |

**Natureza do dado:** CPF e situação cadastral, CND federal, certidão de dívida ativa estadual, certidões cíveis — de **pessoas físicas nomeadas nos próprios nomes de arquivo**, incluindo uma subpasta *"Certidões Cônjuge"*. São **terceiros** de uma operação de retrofit: não são coautores, não têm relação com a plataforma, e não foram informados de que os documentos alimentariam um sistema de busca corporativo.

**Alcance:** os documentos ficaram recuperáveis por qualquer usuário do ChatND, via busca semântica e `grep_knowledge_files`, entre 03/09 e a remoção.

**Causa raiz (mesma de outros dois casos no mesmo período):** `_scripts/escopo.json` é lista de **inclusão por pasta-mãe, recursiva**. Pasta-mãe nova traz junto tudo que estiver abaixo dela, sem inspeção. Os outros dois: minutas *"NÃO CIRCULAM"* (7 arquivos, **três semanas** no ar, 17/08 a 04/09) e notas fiscais nominais de coautores (23 arquivos, PR #141).

**Agravante estrutural (D25):** os três freios da esteira calculam sobre **remoção**. Nenhum olha para adição. A ingestão indevida não disparou nada.

**Medidas tomadas:** regra de exclusão por pasta (#145) · detector de dado pessoal na entrada, com modo de varredura sobre todo o acervo (#146) · guarda das pastas soberanas no portão do sincronizar (#143).

**Limite conhecido:** o detector varre o **repositório**. Documento que esteja no índice e não no repo (resto de carga antiga) exige a conferência com credencial (`conferir.py`) — não foi executada.

**Execução da remoção (04/09/2026, 17h UTC):** PR #145 mergeado e `sincronizar.yml` disparado manualmente com `confirmar_remocao_em_massa` — necessário porque o plano gerava **8 eventos** de remoção contra um limite de 5, e sem a confirmação a rodada abortaria sem escrever nada. Resultado: **37 arquivos removidos, zero falhas** — 35 de `nd-normas` e 2 de `nd-projetos`; as outras cinco coleções intactas. Foram 37 e não 48 porque **11 nunca chegaram ao índice** (entraram no repo na carga do dia, antes de a regra existir): não estavam expostos, e a regra os barra permanentemente.

**O que a remoção expôs, e não estava na varredura do repositório:** `CNH-e.pdf`, dois extratos bancários (`Fatura_Itau`, `FaturaIndividualAgo`) e três certidões (`CNDT`, `consulta protestos`, `certidão de elementos fiscais`) — **existiam só no índice**. Ver D28.

**Ressalva sobre o armazenamento — inalcançável não é apagado.** O `delete_file=true` (que o `sincronizar` usa) apaga os vetores da coleção, a coleção vetorial própria do arquivo e o **registro em `Files`**, que é onde vive o nome — então nome e conteúdo saem da busca. **O blob no Storage/R2 não é apagado por esse caminho.** Ele fica sem registro que o referencie, portanto inalcançável pela aplicação — mas continua existindo no bucket. Para quem for decidir sobre retenção: **a diferença entre "não é mais recuperável pelo sistema" e "não existe mais" é real**, e só a segunda satisfaz um pedido de eliminação. Decisão não tomada em 04/09.

**Recomendação a quem cuida do `FAZ_DueDiligenceDocumental`:** se um dia esse documento precisar voltar à base, a saída certa **não** é afrouxar a regra — é **remover os CPFs do documento no SharePoint**. A análise dominial cumpre sua função identificando a proprietária pelo nome e pela matrícula; o CPF dela e o do cônjuge não acrescentam nada à análise e são o que impede a indexação. Mesmo raciocínio vale para qualquer documento de análise que cite terceiros.

## Parte 2 — Pendências que precisam de decisão ou ação

### Prioridade definida pelo usuário
1. ✅ **Separação de memória (Postgres)** — CONCLUÍDA em 2026-07-02 (Postgres + pgvector + R2). Ver [07_Diario_e_Status](07_Diario_e_Status.md).
2. 🟠 **Volume / billing do Railway** — o volume antigo segue montado como backup; avaliar desmontá-lo após período de segurança. Billing OK ($3/$20). **Atualização 10/09/2026:** ver D40 — fatura do período em US$ 52,62 (estimativa US$ 94,96), dois volumes Postgres desanexados a apagar.
3. 🟡 **Grupos / permissionamento** — controle de acesso mais fino (plano esboçado: grupos Externo/Interno, base pública sem v30).

### Fase 1 (2→7 coleções) — status para o Davi (2026-08-18)
- 🟠 **Publish único** do ciclo Fase 1 (pipe 1.64.0 + fatia 1.63.2): criar as 7 coleções no painel, preencher `MAPA_COLECOES` (json papel→id) e `BASE_CONHECIMENTO_ID`, então publicar. Até lá produção roda a fatia antiga e `MAPA_COLECOES` vazia (dormente).
- ✅ **Etiquetas no contexto injetado** (procedência externa do `nd-externo`, `status=rascunho` do v31, `tipo=convergencia`) — implementadas no 1.64.0 (`_etiquetas_trecho`), entram no mesmo publish.
- ✅ **Fiação do `sincronizar.py`** (T4) — implementada (D-a/D-b/D-c): partição por `colecao_destino` + espelho + freio proporcional; dormente até trocar o `sync_config.json` pelo exemplo da Fase 1.
- 🟡 **Confronto repo × produção**: rodar `migrar_sete_colecoes.py` com `NIDUM_URL`+`NIDUM_TOKEN` (GET) para a lista nominal do que existe só de um lado (o Davi executa com credencial).
- 🟢 **`id vivo` de `BASE_CONHECIMENTO_ID`**: o default morto virou vazio; o id vivo mora no painel.
- 🟢 **`nd-fonte` = 9 obras canônicas** (decisão do Davi): FONTE é curada e pequena; a base nova espelha o repo. Os **61** arquivos legado da "ND Fonte" de produção (só em produção, não no repo) **não migram** — ficam na coleção antiga. Catálogo para revisão no fim do projeto: `esteira/_docs/FASE1_CATALOGO_LEGADO_FONTE.md` (grupo ⭐ = possível doutrina a trazer para o repo).
- ✅ **CORREÇÃO (2026-08-23) do item acima — os 61 JÁ ESTÃO no repo.** Conferência nome a nome (normalizando maiúsculas/pontuação): dos 61 listados no catálogo, **zero** estão ausentes do `FONTE/` do repo (hoje **72** `.md` = os 61 + 8 canônicas + 3 obras que chegaram depois do catálogo). Não foi migração manual: o `git log -- FONTE/` mostra só `puxar_sharepoint: carga automatica (delta)` — entraram no SharePoint e a esteira puxou pelo caminho certo. Cruzamento independente: o `_estado/sharepoint_delta.json` lista os mesmos 70. **Pendência que a conferência levantou:** as canônicas eram 9 e hoje são 8 — o `Nidum Documento Fundador - v31 (rascunho).md` foi removido do repo pela própria esteira (commit `ac3052e`), espelhando uma remoção no SharePoint. Confirmar se foi intencional. **Consequência:** a coleção antiga "ND Fonte - Fundadores" deixou de ser cópia única de qualquer coisa, o que remove o principal impedimento para aposentá-la (ver D19). O catálogo na esteira foi carimbado.
- 🟢 **Higiene (sem agir agora):** 4 comentários pré-existentes não-ASCII no `chatnd.py` (linhas ~298/329/411/1561) violam a convenção ASCII-only de `_nidum_tools/*.py`; antecedem a Fase 1. Corrigir no **próximo ciclo de manutenção do pipe**, não agora.

### Retenção de conversas (spec decidida 2026-07-02 — a IMPLEMENTAR)
- **Objetivo:** gestão de armazenamento.
- **Regra 1 — comprimir:** chat **sem interação há 7 dias** → comprimir o conteúdo para ocupar menos espaço.
- **Regra 2 — deletar:** chat **sem interação há 90 dias** → deletar o chat **+ os arquivos associados**.
- **Natureza:** funcionalidade nova de backend (Open WebUI não tem nativo) — job agendado sobre a tabela `chat` do Postgres, com **dry-run** antes de qualquer exclusão. A parte dos 90 dias é destrutiva → backup/confirmação.
- **Decisão em aberto:** "comprimir" = compressão real do conteúdo (exige o app descomprimir na leitura, mexe no fork) **ou** arquivar (nativo, mais simples). A decidir antes de implementar.

### `MODELO_GERAL` — ENCERRADO em 05/09/2026: não havia nada para trocar

> **A conclusão inverte o que três pessoas achavam.** O wrapper `nidum-10---dia-a-dia`
> **nunca foi revogado — foi renomeado.** Hoje ele se chama **“Nidum 1.0 - Geral”** no
> painel, e o **id continua o mesmo**. A valve está correta, o default no código está
> correto, e as três saídas que eu havia proposto respondiam a um problema que não existe.

Verificado pelo Davi, abrindo o modelo no painel. Registrado como caso do D24 — ver **D39**.

### Auditoria de custo do Railway (09–10/09/2026) — ações pendentes

Contexto e números em **D40–D43**. Cada item diz **onde** se faz (painel, CLI, PR) e o que já foi simulado.

| Ação | Onde | Estado |
|---|---|---|
| **`RAG_EMBEDDING_BATCH_SIZE` 1 → 64** | **Painel** (Admin → Documentos → Embedding Batch Size). É config persistente no banco (`rag.embedding_batch_size = 1`); variável do Railway **não** tem efeito (D24). | Proposto. Risco de subir: uma requisição que falhe derruba 64 chunks de uma vez (o código honra `Retry-After` e reenvia a requisição inteira); limite da OpenAI é 2.048 entradas e 300 k tokens por requisição — 64 × ~500 tokens = ~32 k, folga de 9×. 128 também cabe; 64 é o ponto em que o tempo de sync cai ~60× sem chegar perto do teto. |
| **Embedding duplo na esteira** | Não há endpoint que vincule à base sem recalcular: `POST /knowledge/{id}/file/add` exige `file.data` (arquivo processado, logo já embutido em `file-<id>`) e chama `process_file(collection_name)`, que reaproveita o **texto** e recalcula o **vetor**. Upload com `process=false` não serve — o `file/add` recusa (`FILE_NOT_PROCESSED`). | **Sem PR.** Contornar = patch no fork: em `process_file`, quando há `collection_name` e a coleção `file-<id>` existe, **copiar as linhas** de `document_chunk` (vetor incluído) em vez de reembutir — ~40 linhas em `routers/retrieval.py` + teste. Custo de não fazer: US$ 0,25 por resync completo e o dobro do tempo — com o lote em 64 o tempo deixa de importar. Reabrir só se o sync voltar a ser gargalo. |
| **Limpeza do armazenamento** (script com simulação por padrão, D27) | `_nidum_manutencao/limpeza_armazenamento.py`, roda **dentro do container** via `railway ssh` (transporte em base64, como o runbook de threads). Três modos, todos simulados em 10/09: **`orfaos-locais`** 2.600 arquivos / 90,1 MB (download vem do R2 — `Storage.get_file` baixa a cada pedido — apagar a cópia local não quebra nada); **`gerados-antigos --dias 30`** 51 arquivos / 13,9 MB (PPTX/HTML/PDF/DOCX/XLSX do gerador, mesma retenção do áudio TTS); **`anexos-antigos --dias 60`** 44 anexos / 1.226 chunks / ~24,8 MB da tabela (só o índice; arquivo fica no R2). | **Executado em 10/09** (aprovado pelo Davi): `orfaos-locais` apagou 2.600 (uploads de 742 MB/4.228 arquivos para 637 MB/1.577); `gerados-antigos` apagou 51. `anexos-antigos` **não** rodou: a lista dos 44 foi mostrada ao Davi (9 PDFs dos livros do fundador de 07/07, 16 PDFs de convergência, brandbook, 3 txt, 14 arquivos de fonte tipográfica). Semanal no Actions (PR #67: gerados real; anexos sob `LIMPEZA_ANEXOS_REAL`). **Atenção no `anexos-antigos`:** entre os 44 estão os PDFs dos livros do fundador anexados em 07/07 (v29/v30) — o script tira **só o índice**, o arquivo fica; ainda assim, é a lista da pendência "PDFs órfãos do v29/v30" abaixo — decidir junto. Se um dia fizer sentido: as coleções `file-*` dos 731 arquivos que **estão** em bases são 10.453 chunks (~200 MB) duplicando o índice das bases — não tocar sem provar que a seleção de arquivo individual na conversa não depende delas. |
| **Backup: PITR do Railway × `pg_dump` diário para o R2** | Decisão do Davi. **Causa do #79:** o teste mensal de restauração não prova nada desde 09/07 — `TESTE_DATABASE_URL` (secret de 09/07 23:07) aponta para um Postgres descartável que **já não existe** (proxy `hayabusa:27149`; o volume dele é o `postgres-volume-U9Rg` desanexado). O dump em si baixou (118 MB) e não foi testado. | **Decidido em 10/09** (Davi): **PITR primário** (provado em 10/09: `Bucket wired: yes`, 16 conjuntos, restaurável até o minuto) **+ dump semanal ao R2** + teste de restauração num Postgres **dentro do runner** (PR na esteira; #79 fechado com a causa). O que se perde desligando o dump de vez: (1) a única cópia **fora do Railway** (bloqueio de conta, fatura, bucket apagado por engano); (2) retenção de 30 dias contra 6 do PITR (`railway postgres pitr schedule` — pode subir); (3) portabilidade — `pg_restore` roda em qualquer lugar em minutos, o PITR restaura **só para um serviço novo no Railway**; (4) o único teste de restauração que existe — o PITR nunca foi restaurado. |
| **Painel: apagar `postgres-volume` e `postgres-volume-U9Rg`; apagar `tts-thalita`; expirar os 6 backups manuais "never" de 06–07/07; definir limites de gasto** | Painel/CLI, pelo Davi. Comandos: `railway volume delete`, `railway postgres pitr backup delete <id>`, `railway usage limit set --target workspace --soft 60 --hard 120`. Hoje: sem soft/hard limit no workspace (só o agente tem hard de US$ 20). | Guiado passo a passo na sessão de 10/09. |
| **Rotação da credencial do bucket WAL** (`WAL_ARCHIVE_KEY/SECRET` do `Postgres-EM0i`) | `railway variables` imprime em claro; apareceu no log de uma sessão do Claude em 09/09. Precaução, não incidente. `railway bucket credentials --bucket stocked-cup-MSag --reset` (pede 2FA) → atualizar as duas variáveis no serviço Postgres (redeploy do banco, ~1 min) → `railway postgres pitr status` deve seguir com `Archiver: enabled` e `Last archived` avançando. **Não** usar `pitr disable/enable` (pode criar bucket novo e perder o histórico de WAL). | Pendente, pelo Davi. |
| **Reranker por API (D42)** | **Adiado em 10/09**: fica local até alguém ler a política de retenção de Jina/Cohere. Quando reabrir: sete testes da Fase A antes, trocar no painel, sete depois, só então apagar o modelo do volume. | Adiado. |
| **`Postgres-PITR` (bucket vazio)** | `railway bucket delete --bucket Postgres-PITR` — custo zero, mas confunde. Conferir antes com `railway postgres pitr status` que o bucket ligado é o `stocked-cup-MSag` (era, em 10/09). | Pendente, pelo Davi. |

### Fase E — o preset agêntico é a próxima versão do ChatND; o que hoje só o pipe faz (registro de 10/09/2026)

**Decisão do Davi:** o preset `ChatND Agente` (Fase A, 03/09) **não é artefato de teste — é a próxima versão do ChatND**. A Fase E é o corte. A lista abaixo é o que o `chatnd.py` 1.65.0 faz **fora** do que o agente nativo já cobre (tool-calling, `list_knowledge_bases`, busca nas bases), para o Davi decidir **o que importa antes do corte**. Fonte: changelog e valves do pipe.

| # | O que só o pipe faz hoje | Onde no pipe |
|---|---|---|
| 1 | **Classificador de rota** (gpt-5-mini) com as travas: geral × documentos × arquivo × imagem, gatilhos de anexo, "refaça mantendo o conteúdo" não vira arquivo, áudio detectado antes do classificador | `ROUTER_MODEL`, travas 1–5 |
| 2 | **Wrappers invisíveis** (`MODELO_GERAL`, `MODELO_DOCUMENTOS`): o usuário vê uma IA só (D2, D5, D7) | valves de modelo |
| 3 | **RAG com cotas** FONTE × ACERVOS (`DIAL_COTA_*`), `MAPA_COLECOES` por assunto, `MAPA_K_POR_COLECAO`, **etiquetas de procedência** no contexto injetado (externo, rascunho, convergência), teto `MAX_CHARS_TOTAL` | DIAL, `_etiquetas_trecho` |
| 4 | **Âncora dos fundadores** quando a busca vem vazia (`ANCORA_FUNDADORES_SE_BUSCA_VAZIA`, `FUNDADORES_MAX_CHARS`) e **tríade** fonte/forma/fluxo só em pedido gerativo (D9) | valves |
| 5 | **Termos canônicos** e atalho de saudação | `TERMOS_CANONICOS`, `ATALHO_SAUDACAO` |
| 6 | **Geração de arquivos** (pptx/docx/xlsx/pdf/html) com layout da marca no código (D4), estrutura por gpt-5.1, **imagens anexadas entram no arquivo**, round-trip de edição lendo os **bytes brutos** do Storage | `gerador_de_arquivos_nidum`, `_completar_anexos` |
| 7 | **Imagem** via Gemini (motor oculto, D8), refino assistido por visão com anexo, sem marcas de terceiros | `IMAGEM_PROMPT` |
| 8 | **Áudio anexado** transcrito (Whisper local) e roteado como texto | rota de áudio |
| 9 | **Saída de voz** (OpenAI TTS) com player no chat, keepalive SSE, cancelamento limpo e **retenção de 30 dias** dos mp3 | `TTS_*`, `_prune_audios` |
| 10 | **Busca web** (Tavily direto) na rota geral, com janela "recente" | `WEB_*` |
| 11 | **Auditoria de tokens / analytics** anônima com salt (`chatnd_analytics.db`) | `ANALYTICS_*` |
| 12 | **Anexos**: `MAX_CHARS_ANEXO`, acervo reduzido quando há anexo (`MAX_CHARS_ACERVO_COM_ANEXO`), "substitui, não soma" | fatia de anexos |
| 13 | **Canal do projeto** (pasta = instruções + coleções + arquivos avulsos, bloco "Material do projeto" como SYSTEM, `MAX_CHARS_PROJETO`) | 1.65.0, `_contexto_projeto` |
| 14 | **Diagnóstico**: `MOSTRAR_ROTA`, `DEBUG_TRECHOS`, logs sem teor | valves |

O que o agente já cobre ou torna desnecessário: roteamento por rota fixa (o modelo escolhe a ferramenta), a listagem de bases (corrigida no #66 para nunca esconder uma base), e a parede geral × documentos por procedência declarada (D23).

### Outras pendências em aberto
| Pendência | Natureza | Observação |
|---|---|---|
| **Default de `BASE_CONHECIMENTO_ID`** aponta para base morta (`f2c8a48c`) | Dívida técnica | Só funciona porque a valve sobrescreve para `a85d8a8f`. Alinhar o default no código. |
| **PDFs órfãos do v29/v30** (cópias fora da base do ChatND) | Decisão | Mantidos por regra `#não exclua nada`; remover só com OK explícito. |
| **`Capture001.png` (9,45 MB)** no volume | Decisão | Não removido (screenshot ambíguo, possível anexo de chat). |
| **Mecanismo de higiene de duplicados recorrente** | Implementação | **Desenhado** em `_nidum_manutencao/HIGIENE_DUPLICADOS.md`, não implementado (em stand-by por decisão do usuário). |
| **Editorial F3 — vínculo "modelo por projeto"** | Implementação + deploy | Núcleo da ficha pronto; falta o manifold no seletor. |
| **Editorial F2.4b — imagens nos exports** | Implementação | Embutir imagens com alt-text nos `.docx/.epub/.pdf`. |
| **`origin/main` atrás do main local** | Sincronização | Um deploy foi via `railway up` do local; sincronizar o GitHub com `git push origin main`. |
| **Repasse no roteador** (rota documentos sem trecho relevante deveria cair para diaadia) | Implementação | Hoje, sem contexto relevante, ainda manda para Documentos sem handoff. |
| **Sobreposição rápido × dia a dia** | Decisão | Fronteira tênue; usuário cogitou afiar ou fundir. |
| **SharePoint como fonte (auto-update)** | Projeto futuro | Exige app no Azure AD + Microsoft Graph + job de sync. |
| **Modelo guard de input/output** | Segurança | Recomendado antes de abrir uploads/conteúdo externo (injeção indireta). |
| **Pipe × arquivos da pasta** (D20) | Implementação | O middleware injeta os arquivos da pasta em `form_data['files']` a **cada** mensagem, e o `_anexos_recentes` não distingue origem. Três riscos: TRAVA 5 (anexo + transformação → rota `arquivo`) disparando sozinha; `MAX_CHARS_ANEXO` sendo pago em todo turno; e a regra "acervo pulado quando há anexo" desligando o RAG institucional em silêncio dentro de um projeto. Conserto proposto: marcar os arquivos vindos da pasta (cruzar ids com `folder.data['files']` via `__metadata__`) e tratá-los como canal próprio — "material do projeto" ≠ "anexo deste turno". **Medir antes de mexer** (log: `classificador=`, `chars_acervo`, linha "COM anexo"). |
| **12 bases — painel** (D83) | Painel (Davi) | `MAPA_COLECOES` = as 11 de hoje + `"projetos": "accc1ac9-1cb3-42c6-b7ea-b9e92753ecc4"` · apagar a base `Ecossistemas` (323 arquivos duplicados) · `MAPA_K_POR_COLECAO` fica em 8. |
| **Cronogramas da Fazenda em duas versões** (D84) | Origem (SharePoint) | `EGP/…/1.1 Cronogramas/` e `0 - Projetos/…/0. Cronograma/` têm os mesmos 7 `FAZ_Cronograma_*` com corpos diferentes. Enquanto as duas cópias existirem, o chat pode citar a versão errada. Conserto na origem, ou `NAO_INDEXAR.txt` na subpasta. |
| **ChatND reiniciou sob carga de indexação** (D83) | Investigação | 323 arquivos em ~11 min (17/09 01:42–01:53 UTC) → 502 e reinício. A Rodada A de 05/09 (379) passou. Ver logs do Railway daquela janela antes da próxima carga grande. |
| **Ordem Projetos > Reuniões > Fonte > Ecossistemas no pipe** (D82) | Decisão + fatia no pipe | Hoje o pipe intercala as bases do `MAPA_COLECOES` em ordem de id, com k igual. Proposta: iterar na ordem do JSON e/ou k por base — **7 testes da Fase A antes e depois**. Alternativa: só no preset agêntico, via system prompt. |
| **EGP em Gestão de Projetos ou base própria?** (D83) | Decisão | Foi para Gestão de Projetos (`pastas_mae`). Base própria = criar no painel e trocar uma linha no `sync_config.json`. |
| **Purga do histórico git dos 61 `.md` de Coautores Intensivo** (D82) | Decisão | Removidos do repo (`chatnd-conhecimento`, commit separado no PR do eixo); seguem no histórico de um repo privado. Originais intactos no SharePoint. |

> Conforme as decisões forem tomadas, mover da Parte 2 para a Parte 1 (com a data e o porquê).
